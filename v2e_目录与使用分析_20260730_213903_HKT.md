# 当前目录与 v2e 使用分析

- 生成时间：2026-07-30 21:39:03 HKT（UTC+08:00）
- 分析目录：`/home/robbie/tyf_code/EventCode/myFEFibreSR`
- v2e 代码目录：`/home/robbie/tyf_code/EventCode/myFEFibreSR/v2e`
- 分析依据：当前工作树源码、README、示例脚本、输入视频元数据，以及本机短转换实测

## 1. 先说结论

这个目录的主体是 `v2e`：它把普通强度视频或图像序列转换成模拟 DVS（Dynamic Vision Sensor）事件。其核心流程是：

```text
普通视频/图像序列
    -> 截取、裁剪、缩放、灰度化
    -> SuperSloMo 插帧（可关闭）
    -> 有状态的 DVS 像素模型
    -> 事件流 [时间, x, y, 极性]
    -> HDF5 / AEDAT2 / AEDAT4 / 文本
    -> 可选：把事件重新累积成 AVI 预览视频
```

实际使用时，最重要的是分清四组时间参数：

1. `--start_time`、`--stop_time`：选择源视频的哪一段。
2. `--input_frame_rate`、`--input_slowmotion_factor`：定义源帧代表的真实时间。
3. `--timestamp_resolution`、`--auto_timestamp_resolution`、`--disable_slomo`：决定事件模拟所用的时间采样间隔，也决定插帧量和主要计算成本。
4. `--dvs_exposure`、`--avi_frame_rate`：只决定事件预览 AVI 如何成帧、如何播放，不提高事件本身的时间精度。

推荐先用 0.5--3 秒片段、较小输出分辨率和 HDF5 输出做试跑。确认事件率、时间戳和阈值合适后，再处理完整视频。

## 2. 当前目录内容

### 2.1 顶层

| 路径 | 内容 | 作用 |
|---|---|---|
| `v2e/` | 约 268 MiB（不含 `.git`） | v2e 源码、模型、样例视频、脚本和文档 |
| `Foward/` | 约 24 MiB，3 个 PDF | v2e 论文，以及 Neuromorphic High-Throughput Imaging 主文和补充材料 |
| `分辨率板.png` | 约 132 KiB | 分辨率测试图，可作为成像/超分辨实验输入资产 |

`Foward/` 中的材料包括：

- `v2e_2021_...CVPRW.pdf`：v2e 原始论文，介绍有限光感受器带宽、像素阈值失配、漏电事件和散粒噪声模型。
- `Wang et al. - 1 Neuromorphic High-Throughput Imaging...pdf`：Neuromorphic Super-Resolution（NeuroSR）相关主文。
- `supp - Neuromorphic High-Throughput Imaging.pdf`：上述工作的补充材料。

### 2.2 `v2e/` 内部

| 路径 | 作用 |
|---|---|
| `v2e.py` | 主命令行入口，组织输入、插帧、事件模拟与输出 |
| `v2ecore/v2e_args.py` | 全部通用命令行参数及当前默认值 |
| `v2ecore/emulator.py` | DVS 像素模型，真正生成事件 |
| `v2ecore/slomo.py` | SuperSloMo 光流估计与插帧 |
| `v2ecore/renderer.py` | 把事件按时间或事件数重新累积成视频帧 |
| `v2ecore/dataloader.py`、`model.py` | 插帧模型的数据加载和网络结构 |
| `v2ecore/output/` | 文本、AEDAT2、AEDAT4 输出器 |
| `scripts/` | 合成输入、噪声、CSDVS 等示例；部分脚本参数名较旧 |
| `dataset_scripts/` | DDD17/20、ILSVRC、UCF101 数据集转换工具 |
| `input/` | SuperSloMo 权重和 3 个本地样例视频 |
| `media/` | README 图片、动画、公式和统计素材，不是运行必需输入 |
| `test/` | 少量旧测试；不是完整的自动测试套件 |
| `tyf_test/` | 本地 OpenCV 视频尺寸读取测试 |

### 2.3 自带输入资产

| 文件 | 编码/尺寸 | 帧率与帧数 | 容器时长 | v2e 按源码计算的时间跨度 |
|---|---|---:|---:|---:|
| `input/raw_example.avi` | rawvideo，750x750 | 14 FPS，100 帧 | 7.142857 s | `(100-1)/14 = 7.071429 s` |
| `input/super_res_data.mp4` | MPEG-4，1280x960 | 10 FPS，8 帧 | 0.800000 s | `(8-1)/10 = 0.700000 s` |
| `input/tennis.mov` | H.264，1280x720 | 约 59.94 FPS，1551 帧 | 25.875844 s | 约 25.86 s |
| `input/SuperSloMo39.ckpt` | 约 151 MiB | - | - | 灰度 SuperSloMo 模型权重 |

这里“容器时长”和 v2e 的时间跨度不同，是因为 v2e 把首帧放在 `t=0`，末帧放在 `(N-1)/fps`，而视频容器常把 N 个帧周期都计入时长。

## 3. 当前代码版本和本地改动

- Git 分支：`master`
- 当前提交：`63e2189e9adb7abaf0e5ddb870f1ef7360eeffb3`
- `git describe`：`v2e-1.7.0-4-g63e2189-dirty`
- `setup.py` 内部版本仍写成 `1.5.1`，不能据此判断实际代码版本。
- 工作树已有本地修改和 IDE 文件变化；本文没有还原它们。

与上游当前提交相比，影响运行的本地改动主要有：

| 项目 | 上游值/行为 | 当前目录实际值/行为 |
|---|---|---|
| `--avi_frame_rate` 默认值 | 30 FPS | **5 FPS** |
| `--dvs_exposure` 默认值 | `duration 0.01`（100 FPS） | **`duration 0.02`（50 FPS）** |
| Pillow resize | `Image.ANTIALIAS` | 部分路径改为 `Image.LANCZOS` |
| 文本输出 NumPy 类型 | `np.float` | 改为 `float`，兼容新版 NumPy |
| 主程序 | 无 `tyf` 输出 | 加入多处 `print("tyf ...")` 调试输出 |
| 自动输入尺寸读取 | 原代码访问自定义属性 | 当前视频路径改用 OpenCV `cap.get(...)` |

因此，README 中“输出 AVI 30 FPS”和“默认 10 ms 暴光窗口”等描述不适用于当前工作树。

## 4. 本机环境状态

用户指定并已验证的解释器：

```bash
conda activate v2e
which python
# /home/robbie/miniconda3/envs/v2e/bin/python
```

该环境实测为：

- Python 3.10.16
- PyTorch 2.5.1
- torchvision 0.20.1
- OpenCV 4.10.0
- NumPy 1.24.0
- h5py 3.12.1
- numba 0.60.0
- `dv_processing` 1.7.9
- `v2e.py --help` 可正常运行
- 系统 GPU：NVIDIA GeForce RTX 4070 Ti SUPER，16 GiB

一次独立探测曾返回 `torch.cuda.is_available() == False`，随后实际转换进程正常显示 `torch device is cuda`。开始长任务前建议总是重新确认：

```bash
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

默认 Conda `base` 环境是 Python 3.12.7，且缺少 `torch`、`cv2`、`h5py`、`numba`、`argcomplete` 等依赖，不应拿它运行本项目。

注意：`requirements.txt` 是很老且偏 Windows/Jupyter 的完整环境列表；对当前机器更适合使用已有 `v2e` 环境，或按 README 先安装正确 CUDA 版 PyTorch，再执行 `python -m pip install -e .`。AEDAT4 模块会在程序启动时无条件导入 `dv_processing`，新环境即使不输出 AEDAT4，也可能需要安装该包。

## 5. 当前机器已验证的最小命令

以下命令已在本机成功执行。它关闭插帧和预览，只生成 HDF5 事件，适合作为管线 smoke test：

```bash
conda activate v2e
cd /home/robbie/tyf_code/EventCode/myFEFibreSR/v2e

python v2e.py \
  --input input/super_res_data.mp4 \
  --output_folder output/smoke \
  --overwrite \
  --unique_output_folder false \
  --disable_slomo \
  --skip_video_output \
  --no_preview \
  --dvs346 \
  --dvs_h5 events.h5 \
  --dvs_emulator_seed 1 \
  --cutoff_hz 0
```

实测时使用默认 `--cutoff_hz 300`，8 帧、代码时间跨度 0.7 s 的视频生成：

- 242,915 个事件，其中 ON 122,520、OFF 120,395（日志取整显示 122.52k/120.40k）
- `events.h5/events`：形状 `(242915, 4)`，类型 `uint32`
- 事件布局：`[timestamp_us, x, y, polarity]`，polarity 为 `0=OFF, 1=ON`
- 时间范围：4,761 us 到 700,000 us
- 输出文件约 1.10 MiB

不过 10 FPS 源帧配 `--disable_slomo --cutoff_hz 300` 会出现严重 IIR 欠采样警告，所以 smoke test 推荐加 `--cutoff_hz 0`。若要保留真实有限带宽模型，则需要插帧或显著降低 cutoff。

## 6. 视频长度和时间设置：最容易出错的部分

### 6.1 源视频总时间

代码定义：

```text
srcTotalDuration = (源帧数 N - 1) / 源 FPS
```

这表示 N 帧提供 N-1 个帧间隔。它与 `ffprobe` 给出的容器 duration 可能相差一个帧周期。

### 6.2 `--start_time` 和 `--stop_time`

两者单位都是秒，选择源文件元数据时间轴上的片段。默认分别为开头和末尾。

当前源码的实际映射不是简单的 `floor(t * fps)`，而是：

```text
start_frame = int(N * start_time / ((N-1)/fps))
stop_frame  = int(N * stop_time  / ((N-1)/fps))
```

然后程序重新定义：

```text
actual_start = start_frame / fps
actual_stop  = stop_frame / fps
processed_source_span = (stop_frame - start_frame) / fps
```

影响如下：

- 起止点会量化到源帧，通常不会精确等于输入秒数。
- 因公式使用 `N` 而不是 `N-1`，靠近视频末尾时可能比直观的 `floor(t*fps)` 多偏一帧。
- 起止帧都包含在读取范围内，但时间跨度是“帧数减一”个间隔。
- 生成的事件时间戳会从 **0** 重新开始，不保留原视频的绝对 `start_time`。
- 代码没有可靠地把超出视频范围的 `stop_time` 截回末帧；不要设置成大于源码计算总跨度的值。
- `stop_time <= start_time` 会报错。

建议先查看视频：

```bash
ffprobe -v error -select_streams v:0 \
  -show_entries stream=width,height,avg_frame_rate,nb_frames,duration \
  -of default=noprint_wrappers=1 input/tennis.mov
```

再用短片段试跑，例如：

```text
--start_time 1.0 --stop_time 3.0
```

这代表选择源视频约 1--3 秒的帧，但输出事件时间仍从 0 开始，事件总跨度约 2 秒。

### 6.3 `--input_frame_rate`

- 视频文件：覆盖文件元数据中的 FPS。
- 图像文件夹：必须显式指定，否则程序退出。
- 一旦指定，视频文件路径中它会覆盖 `--input_slowmotion_factor` 的语义处理，即代码把慢动作因子重置为 1。
- 只在文件元数据错误，或图像序列没有时间信息时使用。

### 6.4 `--input_slowmotion_factor`

它表示“拍摄 FPS / 文件播放 FPS”，默认 1。

```text
真实源帧间隔 = (1 / 文件 FPS) / input_slowmotion_factor
真实处理时长 = 源视频时间跨度 / input_slowmotion_factor
```

例如素材以 120 FPS 拍摄、文件标为 30 FPS 播放，则：

```text
--input_slowmotion_factor 4
```

每帧应代表 `1/120 s`，而不是 `1/30 s`。它不会改变截取帧索引；`start_time/stop_time` 仍按文件时间轴选帧，选出的片段在事件时间轴上再除以 4。

## 7. 时间戳分辨率、插帧和计算量

### 7.1 三种模式

| 模式 | 参数 | 行为 | 适用场景 |
|---|---|---|---|
| 不插帧 | `--disable_slomo` | 事件模拟步长等于真实源帧间隔 | 输入本身已经高帧率；快速检查 |
| 固定目标步长 | `--auto_timestamp_resolution false --timestamp_resolution T` | 固定插帧倍率 | 需要可复现、统一时间精度 |
| 自动插帧 | 默认 `--auto_timestamp_resolution true` | 每个 batch 根据最大光流，使插值帧间运动不超过约 1 像素 | 运动速度变化大，想自动平衡精度与成本 |

固定模式的倍率为：

```text
upsampling_factor = ceil(real_source_frame_interval / timestamp_resolution)
实际步长 = real_source_frame_interval / upsampling_factor
```

例如 60 FPS 普通视频，源帧间隔约 16.67 ms，目标 3 ms：

```text
ceil(16.67 / 3) = 6 倍插帧
实际时间步长约 2.78 ms
```

自动模式下，如果同时指定 `--timestamp_resolution T`，T 是最大允许步长对应的最低插帧倍率；光流仍可要求更高倍率。自动模式并不保证所有 batch 使用相同时间步长。

### 7.2 计算量

主要成本近似正比于：

```text
源视频时长 x 输出像素数 x 插帧倍率
```

因此最有效的提速手段依次是：

- 缩短 `--stop_time - --start_time`。
- 用 `--dvs346` 或更小尺寸，不要默认保留 1280x960。
- 放宽 `--timestamp_resolution`，例如从 0.1 ms 改为 1--3 ms。
- 只需要事件时用 `--skip_video_output --no_preview`。
- 确认 PyTorch 使用 CUDA。
- GPU 显存允许时把 `--batch_size` 从 8 调到 12 或 16；太大可能 OOM，也可能使自动模式因一个高速帧对而抬高整个 batch 的倍率。

### 7.3 与 `--cutoff_hz` 的约束

光感受器低通用离散 IIR 更新。代码建议：

```text
dt <= 0.3 / (2*pi*cutoff_hz)
```

粗略例子：

| cutoff | 建议最大模拟步长 dt |
|---:|---:|
| 15 Hz | 约 3.18 ms |
| 30 Hz | 约 1.59 ms |
| 100 Hz | 约 0.477 ms |
| 300 Hz（当前默认） | 约 0.159 ms |

所以 `--cutoff_hz 300` 对插帧率要求极高。若输入仅 10 FPS 且关闭插帧，dt=100 ms，与模型要求完全不匹配。快速/理想模拟可设 `--cutoff_hz 0`；真实有限带宽模拟则应同时选择足够小的 `--timestamp_resolution`，并留意日志警告。

## 8. `--dvs_exposure` 和 `--avi_frame_rate` 不等于事件精度

`--dvs_exposure` 只控制怎样把已生成事件累积成 `dvs-video.avi` 的一帧：

| 写法 | 含义 |
|---|---|
| `--dvs_exposure duration 0.02` | 每 20 ms 一帧，即 50 个事件帧/秒；这是当前本地默认值 |
| `--dvs_exposure count 5000` | 每累计 5000 个事件输出一帧 |
| `--dvs_exposure area_count 500 64` | 任一 64x64 区域累计到 500 个事件时输出一帧 |
| `--dvs_exposure source` | 每个输入/插值帧对应一个事件可视化帧 |

`--avi_frame_rate` 仅设置这些 AVI 文件的播放 FPS，当前本地默认是 5。它不改变：

- HDF5/AEDAT/文本事件时间戳；
- SuperSloMo 插帧倍率；
- `--dvs_exposure duration T` 的累计窗口。

例如事件帧用 20 ms 窗口得到 50 帧/秒，但 AVI 以 5 FPS 播放，画面会以真实时间的 1/10 速度回放。

当使用 `--skip_video_output` 时，`--dvs_exposure` 仍会被解析，但不影响保存的事件流。

## 9. 最有用的参数

### 9.1 每次运行通常都应该考虑

| 参数 | 当前默认 | 建议 |
|---|---:|---|
| `-i, --input` | 无 | 视频文件或有序图像目录；命令行运行时总是显式给出 |
| `-o, --output_folder` | `v2e-output` | 为每个实验指定明确目录 |
| `--overwrite` | False | 确认可覆盖时使用；否则默认会建立 `-1`、`-2` 目录 |
| `--start_time`, `--stop_time` | 全视频 | 初次先取 0.5--3 秒 |
| `--dvs346` 等 | 无 | 明确指定目标传感器尺寸；常用 `--dvs346` |
| `--auto_timestamp_resolution` | True | 运动变化大时保留默认；固定实验设 False |
| `--timestamp_resolution` | None | 固定模式必填；常从 1--3 ms 试起 |
| `--no_preview` | False | 批处理、SSH 或计时时建议开启 |
| `--skip_video_output` | False | 只要事件数据时开启，可省 I/O 和渲染 |
| `--dvs_h5` / `--dvs_text` / `--dvs_aedat2` | None | 至少选一种，否则默认主要只得到预览视频 |
| `--dvs_emulator_seed` | 0（随机） | 对比实验设非零，如 1 |

### 9.2 DVS 模型核心参数

| 参数 | 当前默认 | 影响 |
|---|---:|---|
| `--pos_thres` | 0.2 | ON 事件对数亮度阈值；越小事件越多 |
| `--neg_thres` | 0.2 | OFF 事件对数亮度阈值；越小事件越多 |
| `--sigma_thres` | 0.03 | 像素间阈值标准差；0 表示完全一致 |
| `--cutoff_hz` | 300 | 光感受器 3 dB 低通；0 关闭有限带宽 |
| `--leak_rate_hz` | 0.01 | 每像素漏电事件率 |
| `--shot_noise_rate_hz` | 0.001 | 暗区 ON+OFF 时间噪声率 |
| `--refractory_period` | 0.0005 s | 同像素不应再次触发的死区；0 关闭 |
| `--leak_jitter_fraction` | 0.1 | 漏电事件间隔抖动 |
| `--noise_rate_cov_decades` | 0.1 | 像素间噪声率的对数正态差异；当前主要作用于 leak |
| `--photoreceptor_noise` | False | 用光感受器噪声机制替代默认 shot-noise 注入；要求 cutoff 和 shot rate 都非零 |

阈值单位是自然对数亮度变化。`0.2` 大致对应亮度上升 `exp(0.2)-1 = 22%` 或下降 `1-exp(-0.2) = 18%`。

`--dvs_params clean` 和 `--dvs_params noisy` 是整套预设，并会覆盖上表中的自定义模型参数：

| 预设 | pos/neg | sigma | cutoff | leak | shot | refractory |
|---|---:|---:|---:|---:|---:|---:|
| `clean` | 0.2/0.2 | 0.02 | 0 | 0 | 0 | 0 |
| `noisy` | 0.2/0.2 | 0.05 | 30 Hz | 0.1 Hz/px | 5 Hz/px | 0 |

需要精确调参时不要传 `--dvs_params`，保留默认 `None`。

### 9.3 尺寸、输入和输出参数

- 预设尺寸：`--dvs128`=128x128、`--dvs240`=240x180、`--dvs346`=346x260、`--dvs640`=640x480、`--dvs1024`=1024x768。
- 自定义尺寸必须同时给 `--output_width W --output_height H`；只给一个时，代码会把两者都重置成 `None`。
- `--crop L,R,T,B` 在缩放前按左、右、上、下像素裁剪，例如 `--crop 100,100,0,0`。
- `--input_frame_rate FPS` 用于图像目录或修正错误视频元数据。
- `--batch_size` 默认 8，主要影响 SuperSloMo 显存、速度和自动倍率估计粒度。
- `--dvs_vid_full_scale` 默认 2，只影响事件预览图的黑白饱和值。
- `--vid_orig None`、`--vid_slomo None`、`--dvs_vid None` 可分别关闭对应 AVI；通常直接用 `--skip_video_output` 更方便。

输出格式：

| 参数 | 格式与注意点 |
|---|---|
| `--dvs_h5 NAME` | 推荐用于 Python/训练；`events` 为 `uint32 [t_us,x,y,p]` |
| `--dvs_text NAME` | 每行 `[t_seconds,x,y,p]`，p 为 0/1；可读但大文件很慢、很占空间 |
| `--dvs_aedat2 NAME` | jAER；仅支持 346x260、240x180、640x480，坐标会按 jAER 约定翻转 |
| `--dvs_aedat4 NAME` | 依赖 `dv_processing`；当前实现把 writer 分辨率固定为 640x480，使用前应特别核对 |
| `--ddd_output` | HDF5 中额外保存帧、帧时间和对应事件索引；主要用于 DDD/DAVIS 双模态数据 |

### 9.4 一般不需要改的实验/调试参数

- `--show_dvs_model_state ...`、`--save_dvs_model_state`：观察并保存内部状态。
- `--record_single_pixel_states x,y`：记录单像素内部状态到 `pixel-states.dat`，最多 10,000 个样本。
- `--slomo_stats_plot`：显示自动插帧统计图，会阻塞等待关图。
- `--label_signal_noise`：标记信号/shot-noise；要求文本或 AEDAT 输出，不能与 `--photoreceptor_noise` 同用，且 shot rate 必须非零。
- `--hdr`：输入已经是对数 HDR 灰度时跳过普通 lin-log 转换，不适用于一般 MP4/MOV。
- `--synthetic_input scripts.xxx`：使用 Python 类生成帧；该类提供自己的时间戳，因此忽略文件输入和 SuperSloMo 时间参数。
- `--cs_lambda_pixels`、`--cs_tau_p_ms`：实验性的 center-surround DVS。
- `--scidvs`：实验性的 SCIDVS 非线性自适应像素模型。

## 10. 推荐命令模板

### 10.1 tennis：固定约 3 ms 事件步长，先跑 3 秒

这个组合与项目 README 思路一致，同时明确使用当前环境和 HDF5：

```bash
conda activate v2e
cd /home/robbie/tyf_code/EventCode/myFEFibreSR/v2e

python v2e.py \
  -i input/tennis.mov \
  -o output/tennis-3s \
  --overwrite \
  --stop_time 3 \
  --dvs346 \
  --auto_timestamp_resolution false \
  --timestamp_resolution 0.003 \
  --cutoff_hz 15 \
  --pos_thres 0.15 \
  --neg_thres 0.15 \
  --sigma_thres 0.03 \
  --dvs_exposure duration 0.005 \
  --dvs_h5 tennis.h5 \
  --dvs_emulator_seed 1 \
  --no_preview
```

60 FPS 输入下约为 6 倍插帧，实际模拟步长约 2.78 ms；15 Hz cutoff 的建议最大步长约 3.18 ms，因此匹配合理。`duration 0.005` 只表示 DVS 预览以 5 ms 窗口成帧。

### 10.2 只生成训练用事件，减少输出开销

```bash
python v2e.py \
  -i input/tennis.mov \
  -o output/tennis-events \
  --overwrite \
  --stop_time 3 \
  --dvs346 \
  --auto_timestamp_resolution false \
  --timestamp_resolution 0.003 \
  --cutoff_hz 15 \
  --dvs_h5 events.h5 \
  --skip_video_output \
  --no_preview \
  --dvs_emulator_seed 1
```

### 10.3 理想、无噪声基线

```bash
python v2e.py \
  -i input/tennis.mov \
  -o output/tennis-clean \
  --overwrite \
  --stop_time 1 \
  --dvs346 \
  --auto_timestamp_resolution false \
  --timestamp_resolution 0.003 \
  --dvs_params clean \
  --dvs_h5 events.h5 \
  --skip_video_output \
  --no_preview \
  --dvs_emulator_seed 1
```

### 10.4 图像序列

```bash
python v2e.py \
  -i /path/to/ordered_frames \
  --input_frame_rate 100 \
  -o output/from-frames \
  --overwrite \
  --dvs346 \
  --auto_timestamp_resolution false \
  --timestamp_resolution 0.001 \
  --dvs_h5 events.h5 \
  --skip_video_output \
  --no_preview
```

目录应只放可读图像，文件名排序必须与时间顺序一致。

## 11. 当前代码中值得注意的问题

1. **默认参数并不总是物理上匹配。** 默认 cutoff=300 Hz 对时间步长要求约 0.159 ms，普通低帧率视频若不大量插帧会欠采样。必须看日志中的 IIR warning。
2. **`--output_in_place` 当前基本不可用。** `--output_folder` 的默认值始终是 `v2e-output`，而工具函数禁止它和 `output_in_place=True` 同时存在；建议显式使用 `-o`，不要依赖此选项。
3. **`--no_preview` 应在批处理中显式给出。** `--skip_video_output` 的帮助声称也会关闭预览，但主程序的 `preview` 变量只直接读取 `--no_preview`。
4. **无头/显示器枚举可能偶发卡住。** 一次实测停在 `screeninfo.get_monitors()` 的 DRM 枚举，重试后 X11 枚举和转换均正常。SSH 或容器中应加 `--no_preview`；若仍卡住，需要修改 `emulator.py`，在不显示内部状态时跳过显示器枚举。
5. **NumPy 2 不完全兼容。** `v2e.py` 和 DDD 脚本仍使用 `np.math.floor`，DDD reader 还有 `np.int`。当前 `v2e` 环境的 NumPy 1.24 可运行；新建环境建议先限制 `numpy<2`，或修代码后再升级。
6. **Pillow 修补不完整。** `dataloader.py` 一处已从 `Image.ANTIALIAS` 改成 `Image.LANCZOS`，另一条数据加载路径仍保留旧 API，较新 Pillow 下可能报错。
7. **AEDAT4 分辨率写死。** `aedat4_output.py` 无论实际输出尺寸都以 640x480 创建 writer，且构造时没有接收主程序传入的尺寸。
8. **起止时间缺少范围钳制。** 不要让 `start_time/stop_time` 越界，否则读取数、时间归一化和实际帧数可能不一致。
9. **日志中的处理帧数会重复计数。** 视频路径先把 `num_frames` 设成待处理数，读取时又逐帧加一；实测 8 帧最终日志显示 processing 16 frames。事件数和事件时间不受这个统计问题影响。
10. **帮助允许参数缩写，且未知参数检查被注释。** `parse_known_args()` 会接受额外参数，拼错普通参数时可能只报警甚至继续；正式实验应保存并检查输出目录中的 `v2e-args.txt`。
11. **旧脚本不一定能直接运行。** `test/v2e-tests.sh` 引用了当前目录不存在的 `input/box-moving-white.mp4`，部分脚本还使用旧缩写参数名；应以 `python v2e.py --help` 和 `v2ecore/v2e_args.py` 为准。

## 12. 建议的实验流程

1. 用 `ffprobe` 核对 FPS、帧数、尺寸和时长。
2. 确认 `torch.cuda.is_available()` 为 True。
3. 用 `--start_time/--stop_time` 取 0.5--3 秒。
4. 固定 `--dvs346`、`--dvs_emulator_seed 1`，先输出 HDF5。
5. 先跑 `--dvs_params clean` 建立理想基线，再使用自定义阈值、带宽和噪声。
6. 检查 `v2e-args.txt`、事件数、每像素事件率和 IIR 警告。
7. 用 Python 检查 HDF5 的时间单调性、坐标范围、ON/OFF 比例。
8. 参数确认后再扩大视频长度；需要训练数据时加 `--skip_video_output --no_preview`。

一个简单的 HDF5 检查片段：

```bash
python - <<'PY'
import h5py
import numpy as np

with h5py.File("output/tennis-events/events.h5", "r") as f:
    e = f["events"][:]

print("shape:", e.shape)
print("time range (s):", e[0, 0] / 1e6, e[-1, 0] / 1e6)
print("monotonic:", np.all(np.diff(e[:, 0].astype(np.int64)) >= 0))
print("x range:", e[:, 1].min(), e[:, 1].max())
print("y range:", e[:, 2].min(), e[:, 2].max())
print("ON ratio:", e[:, 3].mean())
PY
```

## 13. 参数选择速查

| 目标 | 推荐起点 |
|---|---|
| 只确认程序可跑 | `--disable_slomo --cutoff_hz 0 --stop_time 1 --dvs346` |
| 普通 30--60 FPS 视频 | 固定 `timestamp_resolution=1--3 ms`，或自动模式；根据 dt 选择 cutoff |
| 只要训练事件 | `--dvs_h5 events.h5 --skip_video_output --no_preview` |
| 可复现实验 | 固定 resolution、尺寸、阈值、`--dvs_emulator_seed 1`，保存 `v2e-args.txt` |
| 理想事件基线 | `--dvs_params clean` |
| 强噪声/弱光压力测试 | `--dvs_params noisy`，再依据目标相机标定参数 |
| 已有高速/慢动作输入 | 正确设 `--input_slowmotion_factor`，必要时 `--disable_slomo` |
| jAER 回放 | `--dvs_aedat2 name.aedat` 并选 `--dvs346/240/640` |

最关键的原则是：先确定源帧真实时间，再确定事件模拟时间步长，最后才决定预览视频如何累积和播放。不要用 `dvs_exposure` 或 `avi_frame_rate` 去代替 `timestamp_resolution`。
