我打算先做仿真， 通过前向模型得到    frame 和对应的事件流， 然后 呢再利用这个 paper 中的方法就能重建出来超分的光纤图像了， 我现在光纤的参数为 FIGH-15-600N, 15000 纤芯，总直径为 600um， 芯间距大概为 4.5um, 芯的直径大约为 2.9um,

然后我先不做真实的实验采集  frame 和对应的事件流， 而是先仿真， 这样如果我仿真得到 frame 和事件流然后重建得到了高分没有蜂窝的图像了，我再做实验采集，再用上面 paper 中方法重建。   为什么先做仿真呢， 因为前向模型决定了仿真得到的 frame 和对应的事件流， 而这个前向模型其实就是重建的时候我们要使用的。 你理解了吗，  你理解一人家重建的方法， 然后理解我的意思， 然后给出咱们的前向模型， 然后给出我在做端到端多芯光纤仿真的时候需要的  input 数据有哪些， 以及 仿真的时候建议的关键参数

已完成思考



我完全理解你的思路：这是计算成像领域非常标准的**「前向仿真 - 逆向重建」闭环验证逻辑**—— 仿真用的光纤前向模型，和后续 NeuroSR 重建时嵌入的物理前向算子\(f_\Theta\)完全一致，先在数字环境里验证 “帧 + 事件融合确实能消除蜂窝伪影、实现超分辨”，再搭实物实验，能大幅规避硬件踩坑、少走弯路。

而且这套逻辑和原论文的 NeuroSR 框架完美契合：论文核心就是**前向算子\(f_\Theta\)可插拔**，你只需要把 “自由衍射 / 4f 中继” 替换成 “GRIN + 多芯光纤 + 中继物镜” 的传输模型，整套两阶段 MAP 重建、运动估计、联合优化的架构全部可以复用。

下面我从「重建方法对齐 → 完整光纤前向模型 → 仿真输入清单 → 关键参数建议 → 重建对接注意事项」逐层展开，完全匹配你的光纤参数和光路。

------

## 一、先对齐：NeuroSR 重建里，你的光纤前向模型处在什么位置

原论文总优化目标（正文式 2）：

\(O^*, \boldsymbol{v}^*, \Theta^*=\arg\min_{O,\boldsymbol{v},\Theta} \underbrace{\mathcal{D}_F\left(b_F,\ \int_T f_\Theta(O(\boldsymbol{x}-\boldsymbol{v}(t)))dt \downarrow\right)}_{\text{帧保真项}}\)

- \alpha

  

  \underbrace{\mathcal{D}

  E\left(b_E(\boldsymbol{v}),\ -\nabla\log f

  \Theta (O)\cdot \boldsymbol {v}\right)}_{\text {事件保真项}}

- \sum\beta_n\mathcal{R}_n



- O：你要重建的**高清无蜂窝物体图像**（最终目标）
- \(f_\Theta(\cdot)\)：**整条光路的前向算子**，也就是你现在要做的仿真模型；\(\Theta\)是光纤、GRIN、物镜的物理参数
- 仿真生成数据的过程：给定高清O，通过\(f_\Theta\)+ 运动积分得到仿真帧\(b_F\)，通过时序光强变化 + v2e 得到仿真事件\(b_E\)
- 重建的过程：拿到\(b_F,b_E\)，反向优化O和\(\Theta\)，让模拟观测和真实观测尽可能接近

**核心自洽性**：仿真的\(f_\Theta\)和重建的\(f_\Theta\)是同一个模型，验证才有意义。

------

## 二、完整多芯光纤前向模型（对应光路：物体→GRIN→MCF→物镜→传感器）

你的光纤参数先统一整理：

表格







|         参数         |         符号         |  数值  |       备注       |
| :------------------: | :------------------: | :----: | :--------------: |
|       纤芯总数       | \(N_{\text{core}}\)  | ~15000 |  蜂窝六边形排布  |
|      光纤总直径      | \(D_{\text{total}}\) | 600 μm |   有效传像区域   |
| 纤芯间距（晶格常数） |          p           | 4.5 μm | 相邻纤芯中心距离 |
|       纤芯直径       | \(d_{\text{core}}\)  | 2.9 μm | 单根纤芯通光孔径 |

### 层级 1：物面运动（和论文完全一致）

设高清目标物体为\(O(x,y)\)，相对运动速度为\(\boldsymbol{v}=(v_x, v_y)\)（单位：μm/s），则t时刻物面光强为：

\(O_t(x,y) = O(x - v_x \cdot t,\ y - v_y \cdot t)\)

- 实现：图像亚像素平移（双线性插值，保证可微）
- 边界处理：镜像填充 / 零填充，避免边缘黑边

### 层级 2：GRIN 透镜耦合（物面→光纤入射端面）

GRIN 透镜的作用是**把物方视场成像、耦合到多芯光纤的入射端面上**，仿真做工程化简化即可，核心保留 3 个效应：

1. **视场匹配与缩放**：把物面尺寸缩放至光纤端面尺寸（600μm），对应 GRIN 的放大倍率\(M_{\text{GRIN}}\)
2. **高斯模糊像差**：模拟 GRIN 的球差、衍射极限，用高斯核卷积

\(I_{\text{GRIN},t} = O_t * G(\sigma_{\text{GRIN}})\)

1. **边缘耦合效率衰减**：视场边缘光线入射角大，耦合进纤芯的效率低，叠加一个二维高斯暗角因子

\(I_{\text{in},t}(x,y) = I_{\text{GRIN},t}(x,y) \cdot \exp\left(-\frac{x^2+y^2}{2\sigma_{\text{vign}}^2}\right)\)

> 进阶可加：数值孔径 NA 匹配，只有入射角小于光纤 NA 的光线才能耦合；初期仿真可以先省略。

### 层级 3：多芯光纤（MCF）传输（核心离散采样，蜂窝伪影来源）

这是整个前向模型最核心的部分，也是重建要消除的蜂窝伪影的来源。**为了保证重建时可微，不能用硬阈值离散采样，要用软核积分近似**。

#### 3.1 预生成蜂窝纤芯坐标网格

六边形蜂窝排布生成规则：

- 水平方向：每行纤芯间距为\(p=4.5\ \mu\text{m}\)
- 垂直方向：行间距为\(\frac{\sqrt{3}}{2}p \approx 3.897\ \mu\text{m}\)
- 相邻行水平偏移\(p/2\)，形成正六边形晶格
- 总范围：直径 600μm 圆形区域内，生成所有纤芯中心坐标\(\{(x_i, y_i)\},\ i=1,2,...,N_{\text{core}}\)

#### 3.2 纤芯积分采样（可微版本）

每个纤芯对入射光场做孔径积分，输出一个强度值。用**高斯核加权积分**替代硬孔径，全程可微：

对第i根纤芯，输出强度：

\(c_i(t) = \iint I_{\text{in},t}(x,y) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{core}}^2}\right) dxdy\)

其中\(\sigma_{\text{core}}\)由纤芯直径决定：\(d_{\text{core}}=2.9\ \mu\text{m}\)，取\(\sigma_{\text{core}} \approx d_{\text{core}}/2.355 \approx 1.23\ \mu\text{m}\)（对应高斯半高全宽等于芯径）。

#### 3.3 光纤出射光场重建

每根纤芯作为点光源，在出射端面重新渲染成连续光场，同样用高斯核扩散（模拟纤芯出射光的弥散、纤芯间串扰）：

\(I_{\text{out},t}(x,y) = \sum_{i=1}^{N_{\text{core}}} c_i(t) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{crosstalk}}^2}\right)\)

- \(\sigma_{\text{crosstalk}}\)：纤芯串扰系数，代表相邻纤芯的光泄露，初期可取 0.3~0.5 倍芯间距
- 传输衰减：整体乘一个透射系数\(\eta_{\text{fiber}}\)（比如 0.85）

> 关键说明：这一步输出的就是**带蜂窝点阵伪影的光纤出射图像**，是你后续重建要消除的目标。

### 层级 4：中继放大物镜（光纤出射面→传感器面）

中继物镜把光纤出端面放大成像到 DAVIS 传感器上，核心两个效应：

1. **几何放大**：放大倍率\(M_{\text{obj}}\)，坐标缩放
2. **衍射模糊**：物镜的衍射极限，用高斯核卷积近似点扩散函数 PSF

\(I_{\text{obj},t} = \text{resize}(I_{\text{out},t},\ M_{\text{obj}}) * G(\sigma_{\text{PSF}})\)

### 层级 5：DAVIS 传感器面输出（最终光强序列）

1. **坐标映射**：把物镜成像的光场，对齐到 DAVIS346 的像素网格（像素间距 18.5μm）

2. 下采样 + 量化

   ：积分到像素网格，叠加噪声

   - 光子泊松噪声：和光强成正比
   - 高斯读出噪声：固定标准差

3. **输出**：t时刻的传感器光强图\(I_t\)

4. **时序堆叠**：遍历所有时刻，得到连续灰度视频，直接喂给 v2e

------

## 三、端到端仿真的输入数据清单

### 1. 物体输入（高清目标）

表格







|      类别       |         推荐素材         |                      作用                      |
| :-------------: | :----------------------: | :--------------------------------------------: |
|  标准分辨率靶   | 1951 USAF 分辨率靶高清图 | 定量测 MTF、验证超分辨倍数，和论文评价体系对齐 |
|  生物样本仿真   | 细胞切片、组织切片灰度图 |         验证生物场景下蜂窝伪影消除效果         |
|   随机纹理图    |  高斯随机纹理、自然图像  |         普适性测试，避免过拟合特定图案         |
| 棋盘格 / 线条图 |     横竖线对、棋盘格     |         定性观察蜂窝伪影、边缘恢复效果         |

> 要求：物面图像的物理分辨率要足够高，建议**物方采样率≥1μm / 像素**，保证纤芯采样过程准确。

### 2. 光学硬件参数输入

#### （1）多芯光纤（已给，可直接用）

- 纤芯间距 4.5μm、芯径 2.9μm、总直径 600μm、六边形排布
- 透射率、串扰系数（可先设经验值）

#### （2）GRIN 透镜

- 工作距离、放大倍率（匹配物方视场和光纤端面）
- NA 数值孔径（和光纤 NA 匹配）
- 像差高斯模糊标准差（初期可设 0.5~1μm）

#### （3）中继物镜

- 放大倍率M（建议选 10×/20×，让光纤端面铺满 DAVIS 传感器视场）
- NA 数值孔径（决定衍射极限 PSF 大小）

#### （4）DAVIS 传感器

- 像素数：346×260
- 像素间距：18.5μm
- 动态范围、噪声水平（和 v2e 参数匹配）

### 3. 运动参数输入

- 运动方向：建议先做单方向匀速平移（比如水平方向），和论文工业扫描场景一致
- 运动速度：v（单位：μm/s 或 像素 / 秒）
- 总仿真时长：\(\Delta T\)（对应一帧 APS 的曝光时间，比如 25ms，对应 40fps）
- 时间步长：\(\Delta t\)（仿真光强序列的时间间隔，决定输入 v2e 的帧率）

### 4. v2e 工具输入参数

- 输入：你仿真输出的连续光强图片序列
- 事件对比度阈值c（匹配 DAVIS346 默认值）
- APS 帧率：比如 40fps，和原论文一致
- 噪声参数：暗事件率、阈值抖动等
- 输出格式：事件流（h5/txt）+ 同步 APS 帧

------

## 四、仿真关键参数建议（适配 v2e + NeuroSR 重建）

### 1. 空间分辨率设置（最关键，决定仿真精度）

- **光纤端面仿真网格**：建议采样率 ≤ 1μm / 像素（即每根纤芯直径 2.9μm 至少覆盖 3 个像素），保证纤芯积分采样准确
- **物面高清网格**：按 GRIN 放大倍率反推，物方采样率和光纤端面对齐
- **最终传感器面**：严格对应 DAVIS346 的 346×260 像素，像素间距 18.5μm
- **超分辨目标**：初期先做**2 倍超分辨**（和论文一致），即重建图像像素间距是原生的 1/2，验证能突破纤芯间距的分辨率极限

### 2. 运动速度设置（决定事件产生质量）

- 核心原则：**一帧曝光时间内，物体移动 0.5~2 个纤芯间距**，既能产生足够事件，又不会运动过度模糊
- 参考值：
  - 纤芯间距 4.5μm，取一帧移动 1 个纤芯间距 = 4.5μm
  - APS 帧率 40fps，曝光时间 25ms
  - 对应速度：\(v = 4.5\ \mu\text{m} / 25\ \text{ms} = 180\ \mu\text{m}/\text{s}\)
- 速度太慢：事件太少，梯度约束弱，超分辨效果差
- 速度太快：单帧运动模糊过重，且 v2e 事件容易过密失真

### 3. 时间步长设置（v2e 输入帧率）

- 建议仿真光强序列帧率：**1000~2000 fps**
- 原因：事件时间精度是微秒级，输入光强的时间采样越密，v2e 模拟的事件触发时刻越准确；1000fps 对应 1ms 步长，平衡计算量和精度
- 25ms 曝光时间内，会生成 25~50 张光强图，足够 v2e 生成连续事件

### 4. 噪声与失真参数建议

- 纤芯串扰：\(\sigma_{\text{crosstalk}} = 0.3 \times p \approx 1.35\ \mu\text{m}\)，模拟真实光纤的芯间光泄露
- 光子噪声：按真实光强量级加泊松噪声，避免无噪声理想数据导致重建效果虚高
- 读出噪声：高斯噪声，标准差取传感器满量程的 1~3%
- 暗角：GRIN 边缘耦合衰减，边缘强度降至中心的 70~80%

### 5. 重建端参数预设

- 事件保真项权重\(\alpha\)：初期取 0.1~0.5，后续根据效果调优
- 正则项：图像用 TV 总变分，抑制蜂窝噪声、保留边缘
- 运动估计：先用对比度最大化估匀速运动，再联合优化微调

------

## 五、和 NeuroSR 重建对接的核心注意事项

1. **前向模型必须全程可微**

   纤芯采样、高斯核渲染、插值、下采样全部要用可微操作（比如用 PyTorch/TensorFlow 实现），不能用硬索引、二值掩码这种不可微操作；这也是为什么前面推荐用高斯核软积分，而不是硬孔径采样。

2. **事件保真项的计算对象**

   论文里事件保真项约束的是\(\nabla\log f_\Theta(O) \cdot \boldsymbol{v}\)，也就是**传感器面光强的对数梯度**；对应到你的链路，就是\(f_\Theta(O)\)经过整条光纤 + 物镜后的输出光强，再取对数算梯度，和 IWE 做对比。

3. **两阶段流程完全复用**

   - 阶段 1：纯事件对比度最大化估计运动场\(\boldsymbol{v}^*\)（光纤蜂窝伪影是静态的，不会随运动偏移，反而不干扰运动估计）
   - 阶段 2：固定运动，联合帧 + 事件优化高清物体O和光纤参数\(\Theta\)

4. **验证指标**

   - 定量：PSNR、SSIM、MTF（和论文图 2d 一致）
   - 定性：蜂窝伪影消除效果、最细可分辨线对

------

如果你需要，我可以下一步直接给你一份**基于 PyTorch 的可微 MCF 前向模型代码框架**，包含蜂窝网格生成、纤芯采样、物镜放大、运动生成，输出图片序列可直接对接 v2e，同时保证可微能直接接入 NeuroSR 重建。

1. 我真实采集的光纤感觉不像是 六边形的哎，  像圆的， 边缘有的又像椭圆的，    但是如果我使用光纤的 mask, 感觉得考虑这个 mask 在什么场景参数下采集的，  是不是就更复杂了 ？     怎么处理最好
2. 边缘耦合效率衰减   这个点， 我使用的是红光 led 照明哦， 你说这个是有必要 吗， 会对重建有影响吗

已完成思考



### 问题 1：纤芯形状与真实光纤 mask 的处理方案

先澄清一个概念误区：**六边形是纤芯的空间排布方式（蜂窝晶格），而单根纤芯的截面本身就是圆形**，二者并不矛盾。你真实采集看到的 “圆点” 是单根纤芯的通光形状，整体点阵的排列规律依然是六边形；边缘出现椭圆，通常是光纤端面切割倾斜、物镜轴外像差、或者耦合角度偏移导致的投影变形。

关于是否要用真实 mask、复杂度怎么权衡，我建议按「**先原理验证、后实验对齐**」分两阶段处理，完全匹配你 “先仿真闭环、再做实物实验” 的路线：

#### 阶段一：原理验证期（当前阶段）—— 不用真实 mask，用标准圆形纤芯 + 六边形排布

**核心原因：你的首要目标是验证「帧 + 事件融合能否消除蜂窝伪影、实现超分辨」，控制变量比 100% 贴近硬件更重要。**

1. **模型足够准确**：标准圆形纤芯 + 正六边形晶格，已经抓住了多芯光纤传像束最核心的特征 —— 离散采样、周期性蜂窝伪影、纤芯间距决定原生分辨率，完全可以支撑原理验证。
2. **保证全程可微**：这是最关键的一点。真实采集的二值 mask 是硬阈值、不可微的，无法直接嵌入 NeuroSR 的梯度下降优化框架；而用高斯核软积分近似圆形纤芯（前面给的模型），全程可微，能完美对接反向传播重建。
3. **避免引入过多未知变量**：真实光纤 mask 会附带纤芯大小不均、位置偏移、缺陷纤芯、端面灰尘、切割角度差异等大量非理想因素。初期如果全部加进去，重建效果不好时，你无法判断是算法本身不行，还是模型参数不准，不利于排错。

#### 阶段二：实验对齐期（准备搭实物时）—— 提取真实纤芯参数，软建模替代硬 mask

当你要把仿真模型和真实采集数据对齐时，也不建议直接用二值 mask 做前向模型，而是走「**参数提取 → 软核重建**」的路线，既贴近真实又保持可微：

1. **离线采集真实端面图**：用均匀白光 / 红光照明光纤入射端，在传感器端拍一张清晰的光纤端面静态图，得到真实的纤芯光斑阵列。

2. **提取纤芯参数**：通过光斑检测算法，提取每根纤芯的**中心坐标、等效半径、相对亮度、椭圆长短轴**这几个参数。

3. 替换标准晶格

   ：把仿真里的正六边形标准纤芯，替换成你提取的真实位置、真实大小的纤芯阵列，依然用高斯核（椭圆核对应椭圆纤芯）做软积分采样。

   - 椭圆纤芯很容易处理：把各向同性高斯核换成各向异性的二维高斯，对应长短轴即可，依然完全可微。

4. **优势**：既贴合了真实光纤的非理想性，又保持了前向模型的可微性，重建时还可以把纤芯位置、半径作为物理参数 \(\Theta\) 的一部分，做轻微的联合校准。

> 补充：你担心的「mask 在什么场景参数下采集」确实是个坑。真实端面图是在特定照明角度、特定聚焦下拍的，和实际成像时的耦合条件不完全一致，直接硬用反而会引入系统误差；用参数化软模型，反而可以通过重建时的联合优化自动修正偏差，鲁棒性更强。

------

### 问题 2：红光 LED 照明下，边缘耦合效率衰减的必要性与影响

#### 结论先说：

- **原理验证初期：完全可以不加**，不影响核心的蜂窝伪影消除和超分辨效果；
- **后期贴近真实实验：建议加上平缓的边缘衰减**，主要影响帧的低频亮度均匀性，对事件通道几乎无影响。

#### 详细解释：

1. **这个效应的物理来源**

   边缘耦合效率衰减，本质是**视场边缘的光线入射角更大，部分光线超过光纤数值孔径（NA），无法耦合进纤芯**，导致边缘区域的纤芯整体亮度偏低，形成暗角。

   你用红光 LED 照明，属于宽光谱、大角度扩展光源，相比激光，它的角度分布更宽，理论上边缘衰减会更平缓，不会出现陡峭的截止边。

2. **对重建的具体影响（分两个通道看）**

   - 对帧保真项（低频）：有影响

     

     边缘亮度衰减是大尺度的低频渐变，如果仿真里不加，真实采集的帧边缘偏暗，重建时帧保真项在边缘的约束就会失配，容易导致边缘区域重建亮度不准、伪影增多。

     

     但这个影响是全局低频的，不涉及细节纹理，重建时可以通过联合优化一个 “亮度渐变因子” 来自动补偿，容错度很高。

   - 对事件保真项（高频）：几乎无影响

     

     事件只对亮度的

     时域变化

     敏感，边缘暗角是空间上的缓慢渐变，时域上几乎没有变化，基本不会触发事件。事件捕捉的是运动带来的局部纹理梯度，和整体亮度的高低关系很小。

3. **实操建议**

   - 现阶段跑通闭环：先不加，把核心的蜂窝伪影、运动超分逻辑跑通，减少不必要的变量。
   - 后期贴近实验：加一个**平缓的二维高斯暗角因子**就行，不用做复杂的角度 - NA 耦合计算。公式很简单： \(\eta(x,y) = \exp\left(-\frac{x^2+y^2}{2\sigma_{\text{vign}}^2}\right)\) 把 \(\sigma_{\text{vign}}\) 设成光纤总半径的 1.2~1.5 倍，让边缘亮度降到中心的 70%~80% 即可，和真实 LED 照明的衰减趋势基本一致。

------

### 整体最优实施路径总结

1. **当前仿真第一版**
   - 纤芯模型：标准圆形纤芯 + 正六边形排布 + 高斯软积分采样（可微）
   - 照明：均匀亮度，暂不加边缘暗角
   - 目标：跑通「高清物体 → 光纤前向 → 运动视频 → v2e 事件 → NeuroSR 重建 → 消除蜂窝 + 2 倍超分」的完整闭环，用 USAF 靶标测 MTF，验证算法成立。
2. **对接实验前的第二版**
   - 纤芯模型：用真实端面图提取纤芯位置、半径、椭圆度，替换标准晶格
   - 照明：加入平缓高斯暗角，匹配真实亮度分布
   - 目标：让仿真前向模型和真实硬件特性对齐，保证重建算法能直接迁移到真实采集数据上。

如果你需要，我可以下一步把「标准六边形圆形纤芯的可微前向模型」写成完整的 PyTorch 代码片段，包含蜂窝网格生成、运动平移、纤芯采样、物镜下采样，可直接对接 v2e 和后续重建。

咱们就先做第一版， 你按照 你的刚才说的 ， 总结成为一个 md 文件， 给我

已完成思考



# 多芯光纤神经形态超分辨成像仿真方案 V1.0（原理验证版）

**版本说明**：本方案为第一版原理验证仿真，遵循「控制变量、全程可微、先闭环后精细」原则，优先验证「帧 + 事件融合能否消除蜂窝伪影、实现 2 倍超分辨」的核心逻辑，暂不引入真实光纤非理想性与复杂光学效应。

## 一、仿真目标与整体思路

### 1.1 核心目标

基于给定的多芯光纤参数，构建**全程可微的光学前向模型**，模拟生成「同步 APS 帧 + 异步事件流」双模态数据，验证原论文 NeuroSR 框架可直接迁移至光纤成像场景，实现：

- 消除多芯光纤固有的蜂窝点阵伪影
- 突破纤芯间距限制，实现 2 倍像素超分辨
- 输出无蜂窝、高细节的高清物体重建图像

### 1.2 设计原则

1. **标准模型优先**：采用理想圆形纤芯 + 正六边形蜂窝排布，保留光纤成像核心特征（离散采样、周期性伪影、芯间距分辨率上限），排除非理想因素干扰。
2. **全程可微约束**：所有光学操作均采用可微实现（高斯软积分、双线性插值等），确保前向模型可直接嵌入 NeuroSR 的 MAP 联合优化框架。
3. **与论文对齐**：数据格式、评价指标、重建流程完全复现原论文范式，保证方法迁移的严谨性。

## 二、仿真光路与模块划分

### 2.1 完整光路顺序（运算顺序严格一致）

plaintext







```
高清物体O → 横向相对运动 → GRIN透镜简化模型 → 多芯光纤(MCF)传输 → 中继放大物镜 → DAVIS传感器光强序列 → v2e生成事件+APS帧
```

### 2.2 模块与前向算子对应关系

整条光路对应 NeuroSR 框架中的可插拔前向算子 \(f_\Theta(O)\)，\(\Theta\) 为光纤、透镜、物镜的物理参数集合；仿真与重建共用同一套前向模型，保证自洽性。

## 三、核心硬件基准参数

### 3.1 多芯光纤参数（FIGH-15-600N）

表格







|       参数名称       |          符号           |  数值  |         备注         |
| :------------------: | :---------------------: | :----: | :------------------: |
|       纤芯总数       |   \(N_{\text{core}}\)   | ~15000 |   正六边形蜂窝排布   |
|   光纤有效传像直径   |  \(D_{\text{total}}\)   | 600 μm |     圆形有效区域     |
| 纤芯间距（晶格常数） |            p            | 4.5 μm |   相邻纤芯中心距离   |
|      单纤芯直径      |   \(d_{\text{core}}\)   | 2.9 μm |   理想圆形通光孔径   |
|      光纤透射率      | \(\eta_{\text{fiber}}\) |  0.85  | 经验值，整体亮度衰减 |

### 3.2 DAVIS346 传感器参数

表格







|    参数名称    |   数值    |        备注        |
| :------------: | :-------: | :----------------: |
|   像素分辨率   | 346 × 260 |    同原论文硬件    |
|    像素间距    |  18.5 μm  |  原生物理采样间距  |
|    APS 帧率    |  40 fps   | 对应曝光时间 25 ms |
| 事件时间分辨率 |   1 μs    |  v2e 仿真默认配置  |

## 四、逐级前向模型（V1.0 版）

### 4.1 物面运动模块

**物理意义**：模拟物体与光纤探头的横向相对匀速运动，是产生事件差分信号的前提。

**数学定义**：

设参考时刻高清物体为 \(O(x,y)\)，匀速运动速度 \(\boldsymbol{v}=(v_x, v_y)\)（单位：μm/s），则 t 时刻物面光强为：

\(O_t(x,y) = O(x - v_x \cdot t,\ y - v_y \cdot t)\)

**实现方式**：

- 采用双线性插值实现亚像素平移，保证全程可微
- 边界采用镜像填充，避免边缘黑边干扰
- V1.0 仅支持匀速平移，暂不考虑变速、旋转、形变

### 4.2 GRIN 透镜简化模型

**物理意义**：将物面光场耦合进光纤入射端面，实现视场匹配与初步成像。

**V1.0 简化为 3 个效应**：

1. **视场缩放**：将物面尺寸等比例缩放至光纤端面尺寸（直径 600 μm），对应 GRIN 放大倍率 \(M_{\text{GRIN}}\)
2. **像差模糊**：用高斯卷积模拟 GRIN 球差与衍射极限 \(I_{\text{GRIN},t} = O_t * G(\sigma_{\text{GRIN}})\)
3. **均匀透射衰减**：整体乘以透射系数，V1.0 暂不加空间边缘暗角

### 4.3 多芯光纤（MCF）传输模块（核心）

**物理意义**：离散纤芯对入射光场进行空间采样，产生蜂窝点阵伪影，是光纤成像的核心特征，也是重建要消除的目标。

#### 步骤 1：预生成正六边形纤芯坐标网格

生成规则：

- 水平方向：每行纤芯间距为 \(p=4.5\ \mu\text{m}\)
- 垂直方向：行间距为 \(\frac{\sqrt{3}}{2}p \approx 3.897\ \mu\text{m}\)
- 奇数行水平偏移 \(p/2\)，形成正六边形晶格
- 保留直径 600 μm 圆形区域内的所有纤芯，输出所有纤芯中心坐标 \(\{(x_i, y_i)\},\ i=1,2,...,N_{\text{core}}\)

#### 步骤 2：纤芯高斯软积分采样（可微实现）

**不使用硬二值掩码**，采用高斯核加权积分模拟圆形纤芯的通光效应，全程支持梯度反向传播：

对第 i 根纤芯，输出积分强度为：

\(c_i(t) = \iint I_{\text{GRIN},t}(x,y) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{core}}^2}\right) dxdy\)

其中 \(\sigma_{\text{core}} = d_{\text{core}} / 2.355 \approx 1.23\ \mu\text{m}\)，对应高斯半高全宽等于纤芯直径。

#### 步骤 3：光纤出射光场渲染

将每根纤芯的输出强度，重新扩散为连续光场，模拟纤芯出射光的弥散与微弱串扰：

\(I_{\text{out},t}(x,y) = \eta_{\text{fiber}} \cdot \sum_{i=1}^{N_{\text{core}}} c_i(t) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{crosstalk}}^2}\right)\)

\(\sigma_{\text{crosstalk}}\) 为纤芯串扰系数，控制相邻纤芯的光泄露程度。

### 4.4 中继放大物镜模块

**物理意义**：将光纤出射端面放大成像到 DAVIS 传感器靶面，匹配传感器尺寸。

**实现方式**：

1. **几何缩放**：按物镜放大倍率 \(M_{\text{obj}}\) 对光场做双线性插值缩放
2. **衍射模糊**：高斯卷积模拟物镜衍射极限 PSF \(I_{\text{obj},t} = \text{resize}(I_{\text{out},t},\ M_{\text{obj}}) * G(\sigma_{\text{PSF}})\)

> 倍率选取建议：使光纤 600 μm 有效区域铺满传感器横向有效视场，保证空间利用率最优。

### 4.5 传感器采样与噪声模块

**物理意义**：将连续光场离散化为传感器像素输出，叠加真实噪声模型。

**处理流程**：

1. 坐标对齐：将物镜输出光场映射到传感器像素网格（像素间距 18.5 μm）
2. 像素积分下采样：得到每个像素的入射光强
3. 噪声叠加：
   - 光子泊松噪声：与光强成正比，符合成像物理规律
   - 高斯读出噪声：固定标准差，模拟电路读出噪声
4. 输出：t 时刻传感器光强图 \(I_t\)

## 五、仿真输入数据清单

### 5.1 高清物体输入

表格







|     类别      |         推荐素材         |                        用途                        |
| :-----------: | :----------------------: | :------------------------------------------------: |
| 标准分辨率靶  | 1951 USAF 分辨率靶高清图 | 定量测试 MTF、验证超分辨倍数，与原论文评价体系对齐 |
| 线条 / 棋盘格 |   横竖线对、棋盘格图案   |         定性观察蜂窝伪影消除、边缘恢复效果         |
|  生物模拟图   | 细胞切片、组织切片灰度图 |         验证生物场景下的伪影抑制与细节保留         |

> 要求：物方物理采样率 ≥ 1 μm / 像素，保证纤芯采样过程计算准确。

### 5.2 光学系统参数输入

表格







|   模块    |                    参数                    |            建议初值            |
| :-------: | :----------------------------------------: | :----------------------------: |
| GRIN 透镜 |        放大倍率 \(M_{\text{GRIN}}\)        | 按物方视场匹配 600 μm 端面计算 |
| GRIN 透镜 |  像差高斯标准差 \(\sigma_{\text{GRIN}}\)   |             0.8 μm             |
| 多芯光纤  | 纤芯串扰系数 \(\sigma_{\text{crosstalk}}\) |    1.35 μm（0.3 倍芯间距）     |
| 中继物镜  |        放大倍率 \(M_{\text{obj}}\)         |      按传感器尺寸匹配计算      |
| 中继物镜  |   PSF 高斯标准差 \(\sigma_{\text{PSF}}\)   |         1.5 μm（物方）         |

### 5.3 运动参数输入

表格







|     参数     | 建议初值 |                           说明                            |
| :----------: | :------: | :-------------------------------------------------------: |
|   运动方向   | 水平单向 |           与原论文工业扫描场景一致，降低复杂度            |
|   运动速度   | 180 μm/s | 单帧曝光时间内移动约 1 个纤芯间距，平衡事件数量与模糊程度 |
| 单帧曝光时长 |  25 ms   |        对应 40 fps APS 帧率，与 DAVIS 原生参数一致        |
|  仿真总时长  |  25 ms   |               对应单帧曝光窗口内的时序数据                |

### 5.4 v2e 工具配置参数

表格







|      参数       |     推荐值      |                    说明                     |
| :-------------: | :-------------: | :-----------------------------------------: |
|    输入帧率     |    1000 fps     | 仿真光强序列时间步长 1 ms，平衡精度与计算量 |
|   传感器模式    |    DAVIS346     |            与原论文硬件完全对齐             |
|   对比度阈值    |     默认值      |             匹配真实传感器阈值              |
|    输出内容     | APS 帧 + 事件流 |             同步输出双模态数据              |
| 事件输出格式... |                 |                                             |

让你生成一个可以下载的 md 文件啊

已完成思考



我无法直接生成可下载的文件，你可以将下面的完整 Markdown 内容全选复制，保存为 `MCF_NeuroSR_Simulation_v1.0.md` 即可作为本地文档使用。

------

# 多芯光纤神经形态超分辨成像仿真方案 V1.0（原理验证版）

**版本说明**：本方案为第一版原理验证仿真，遵循「控制变量、全程可微、先闭环后精细」原则，优先验证「帧 + 事件融合能否消除蜂窝伪影、实现 2 倍超分辨」的核心逻辑，暂不引入真实光纤非理想性与复杂光学效应。

## 一、仿真目标与整体思路

### 1.1 核心目标

基于给定的多芯光纤参数，构建**全程可微的光学前向模型**，模拟生成「同步 APS 帧 + 异步事件流」双模态数据，验证原论文 NeuroSR 框架可直接迁移至光纤成像场景，实现：

- 消除多芯光纤固有的蜂窝点阵伪影
- 突破纤芯间距限制，实现 2 倍像素超分辨
- 输出无蜂窝、高细节的高清物体重建图像

### 1.2 设计原则

1. **标准模型优先**：采用理想圆形纤芯 + 正六边形蜂窝排布，保留光纤成像核心特征（离散采样、周期性伪影、芯间距分辨率上限），排除非理想因素干扰。
2. **全程可微约束**：所有光学操作均采用可微实现（高斯软积分、双线性插值等），确保前向模型可直接嵌入 NeuroSR 的 MAP 联合优化框架。
3. **与论文对齐**：数据格式、评价指标、重建流程完全复现原论文范式，保证方法迁移的严谨性。

### 1.3 光路总览（运算顺序严格一致）

plaintext







```
高清物体O → 横向相对运动 → GRIN透镜简化模型 → 多芯光纤(MCF)传输 → 中继放大物镜 → DAVIS传感器光强序列 → v2e生成事件+APS帧
```

整条光路对应 NeuroSR 框架中的可插拔前向算子 \(f_\Theta(O)\)，\(\Theta\) 为光纤、透镜、物镜的物理参数集合；仿真与重建共用同一套前向模型，保证自洽性。

## 二、核心硬件基准参数

### 2.1 多芯光纤参数（FIGH-15-600N）

表格







|       参数名称       |          符号           |  数值  |         备注         |
| :------------------: | :---------------------: | :----: | :------------------: |
|       纤芯总数       |   \(N_{\text{core}}\)   | ~15000 |   正六边形蜂窝排布   |
|   光纤有效传像直径   |  \(D_{\text{total}}\)   | 600 μm |     圆形有效区域     |
| 纤芯间距（晶格常数） |            p            | 4.5 μm |   相邻纤芯中心距离   |
|      单纤芯直径      |   \(d_{\text{core}}\)   | 2.9 μm |   理想圆形通光孔径   |
|      光纤透射率      | \(\eta_{\text{fiber}}\) |  0.85  | 经验值，整体亮度衰减 |

### 2.2 DAVIS346 传感器参数

表格







|    参数名称    |   数值    |        备注        |
| :------------: | :-------: | :----------------: |
|   像素分辨率   | 346 × 260 |    同原论文硬件    |
|    像素间距    |  18.5 μm  |  原生物理采样间距  |
|    APS 帧率    |  40 fps   | 对应曝光时间 25 ms |
| 事件时间分辨率 |   1 μs    |  v2e 仿真默认配置  |

## 三、逐级前向模型（V1.0 版）

### 3.1 物面运动模块

**物理意义**：模拟物体与光纤探头的横向相对匀速运动，是产生事件差分信号的前提。

**数学定义**：

设参考时刻高清物体为 \(O(x,y)\)，匀速运动速度 \(\boldsymbol{v}=(v_x, v_y)\)（单位：μm/s），则 t 时刻物面光强为：

\(O_t(x,y) = O(x - v_x \cdot t,\ y - v_y \cdot t)\)

**实现方式**：

- 采用双线性插值实现亚像素平移，保证全程可微
- 边界采用镜像填充，避免边缘黑边干扰
- V1.0 仅支持匀速平移，暂不考虑变速、旋转、形变

### 3.2 GRIN 透镜简化模型

**物理意义**：将物面光场耦合进光纤入射端面，实现视场匹配与初步成像。

**V1.0 简化为 3 个效应**：

1. **视场缩放**：将物面尺寸等比例缩放至光纤端面尺寸（直径 600 μm），对应 GRIN 放大倍率 \(M_{\text{GRIN}}\)
2. **像差模糊**：用高斯卷积模拟 GRIN 球差与衍射极限 \(I_{\text{GRIN},t} = O_t * G(\sigma_{\text{GRIN}})\)
3. **均匀透射衰减**：整体乘以透射系数，V1.0 暂不加空间边缘暗角

### 3.3 多芯光纤（MCF）传输模块（核心）

**物理意义**：离散纤芯对入射光场进行空间采样，产生蜂窝点阵伪影，是光纤成像的核心特征，也是重建要消除的目标。

#### 步骤 1：预生成正六边形纤芯坐标网格

生成规则：

- 水平方向：每行纤芯间距为 \(p=4.5\ \mu\text{m}\)
- 垂直方向：行间距为 \(\frac{\sqrt{3}}{2}p \approx 3.897\ \mu\text{m}\)
- 奇数行水平偏移 \(p/2\)，形成正六边形晶格
- 保留直径 600 μm 圆形区域内的所有纤芯，输出所有纤芯中心坐标 \(\{(x_i, y_i)\},\ i=1,2,...,N_{\text{core}}\)

#### 步骤 2：纤芯高斯软积分采样（可微实现）

**不使用硬二值掩码**，采用高斯核加权积分模拟圆形纤芯的通光效应，全程支持梯度反向传播：

对第 i 根纤芯，输出积分强度为：

\(c_i(t) = \iint I_{\text{GRIN},t}(x,y) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{core}}^2}\right) dxdy\)

其中 \(\sigma_{\text{core}} = d_{\text{core}} / 2.355 \approx 1.23\ \mu\text{m}\)，对应高斯半高全宽等于纤芯直径。

#### 步骤 3：光纤出射光场渲染

将每根纤芯的输出强度，重新扩散为连续光场，模拟纤芯出射光的弥散与微弱串扰：

\(I_{\text{out},t}(x,y) = \eta_{\text{fiber}} \cdot \sum_{i=1}^{N_{\text{core}}} c_i(t) \cdot \exp\left(-\frac{(x-x_i)^2+(y-y_i)^2}{2\sigma_{\text{crosstalk}}^2}\right)\)

\(\sigma_{\text{crosstalk}}\) 为纤芯串扰系数，控制相邻纤芯的光泄露程度。

### 3.4 中继放大物镜模块

**物理意义**：将光纤出射端面放大成像到 DAVIS 传感器靶面，匹配传感器尺寸。

**实现方式**：

1. **几何缩放**：按物镜放大倍率 \(M_{\text{obj}}\) 对光场做双线性插值缩放
2. **衍射模糊**：高斯卷积模拟物镜衍射极限 PSF \(I_{\text{obj},t} = \text{resize}(I_{\text{out},t},\ M_{\text{obj}}) * G(\sigma_{\text{PSF}})\)

> 倍率选取建议：使光纤 600 μm 有效区域铺满传感器横向有效视场，保证空间利用率最优。

### 3.5 传感器采样与噪声模块

**物理意义**：将连续光场离散化为传感器像素输出，叠加真实噪声模型。

**处理流程**：

1. 坐标对齐：将物镜输出光场映射到传感器像素网格（像素间距 18.5 μm）
2. 像素积分下采样：得到每个像素的入射光强
3. 噪声叠加：
   - 光子泊松噪声：与光强成正比，符合成像物理规律
   - 高斯读出噪声：固定标准差，模拟电路读出噪声
4. 输出：t 时刻传感器光强图 \(I_t\)

## 四、仿真输入数据清单

### 4.1 高清物体输入

表格







|     类别      |         推荐素材         |                        用途                        |
| :-----------: | :----------------------: | :------------------------------------------------: |
| 标准分辨率靶  | 1951 USAF 分辨率靶高清图 | 定量测试 MTF、验证超分辨倍数，与原论文评价体系对齐 |
| 线条 / 棋盘格 |   横竖线对、棋盘格图案   |         定性观察蜂窝伪影消除、边缘恢复效果         |
|  生物模拟图   | 细胞切片、组织切片灰度图 |         验证生物场景下的伪影抑制与细节保留         |

> 要求：物方物理采样率 ≥ 1 μm / 像素，保证纤芯采样过程计算准确。

### 4.2 光学系统参数输入

表格







|   模块    |                    参数                    |            建议初值            |
| :-------: | :----------------------------------------: | :----------------------------: |
| GRIN 透镜 |        放大倍率 \(M_{\text{GRIN}}\)        | 按物方视场匹配 600 μm 端面计算 |
| GRIN 透镜 |  像差高斯标准差 \(\sigma_{\text{GRIN}}\)   |             0.8 μm             |
| 多芯光纤  | 纤芯串扰系数 \(\sigma_{\text{crosstalk}}\) |    1.35 μm（0.3 倍芯间距）     |
| 中继物镜  |        放大倍率 \(M_{\text{obj}}\)         |      按传感器尺寸匹配计算      |
| 中继物镜  |   PSF 高斯标准差 \(\sigma_{\text{PSF}}\)   |         1.5 μm（物方）         |

### 4.3 运动参数输入

表格







|     参数     | 建议初值 |                           说明                            |
| :----------: | :------: | :-------------------------------------------------------: |
|   运动方向   | 水平单向 |           与原论文工业扫描场景一致，降低复杂度            |
|   运动速度   | 180 μm/s | 单帧曝光时间内移动约 1 个纤芯间距，平衡事件数量与模糊程度 |
| 单帧曝光时长 |  25 ms   |        对应 40 fps APS 帧率，与 DAVIS 原生参数一致        |
|  仿真总时长  |  25 ms   |               对应单帧曝光窗口内的时序数据                |
| 仿真时间步长 |   1 ms   |         对应 1000 fps 输入帧率，平衡精度与计算量          |

## 五、v2e 工具配置

表格







|     参数     |             推荐值              |           说明           |
| :----------: | :-----------------------------: | :----------------------: |
|  传感器模式  |            DAVIS346             |   与原论文硬件完全对齐   |
|   输入数据   | 仿真生成的 1000fps 灰度图像序列 | 对应传感器面连续光强变化 |
|  对比度阈值  |   默认值（约 0.15 log 单位）    |  匹配真实传感器触发阈值  |
| APS 输出帧率 |             40 fps              |     与原论文实验一致     |
| 事件输出格式 |           .txt / .h5            | 包含 (x, y, t, p) 四元组 |
|   噪声配置   |    开启基础暗噪声与阈值抖动     |    贴近真实传感器特性    |

## 六、输出数据格式

### 6.1 仿真直接输出

1. 高时间分辨率传感器光强图像序列（1000fps）
2. 对应时刻的物面高清真值图（用于重建精度评估）

### 6.2 v2e 输出

1. **APS 灰度帧**：40fps，对应 NeuroSR 的帧保真项输入 \(b_F\)
2. **异步事件流**：微秒级时间戳，对应 NeuroSR 的事件原始数据，用于生成 IWE

## 七、验证与评价指标

### 7.1 定量指标

- **PSNR / SSIM**：重建图像与高清真值的相似度
- **MTF（调制传递函数）**：径向平均功率谱，验证超分辨倍数与高频恢复能力
- **蜂窝伪影抑制比**：重建前后周期性伪影的能量占比

### 7.2 定性指标

- USAF 靶标最细可分辨线对组别
- 边缘锐利度与纹理细节保留程度
- 蜂窝点阵伪影的视觉消除效果

## 八、实施步骤建议

1. **步骤 1**：实现正六边形纤芯坐标生成与单张光纤前向成像，验证蜂窝伪影效果
2. **步骤 2**：加入运动模块，生成连续时序光强序列
3. **步骤 3**：对接 v2e，生成同步帧与事件流
4. **步骤 4**：将前向模型嵌入 NeuroSR 框架，验证两阶段重建流程
5. **步骤 5**：定量测试超分辨效果与伪影抑制能力，完成原理验证