# 第一阶段多芯光纤 Frame–Event 仿真最终方案

- 文档版本：Final Phase-1 v1.0
- 生成时间：2026-08-01 09:59:10 HKT（UTC+08:00）
- 工作目录：`/home/robbie/tyf_code/EventCode/myFEFibreSR`
- 目标光纤：Fujikura FIGH-15-600N
- 对照文档：[豆包方案](多芯光纤Frame-Event端到端仿真与NeuroSR重建方案-豆包.md)、[上一版方案](多芯光纤Frame-Event端到端仿真与NeuroSR重建方案.md)
- 论文依据：[NeuroSR 主文](Paper/Wang%20et%20al.%20-%201%20Neuromorphic%20High-Throughput%20Imaging%20via%202%20Trans.pdf)、[NeuroSR 补充材料](Paper/supp%20-%20Neuromorphic%20High-Throughput%20Imaging.pdf)、[v2e 论文](Paper/v2e_2021_2021%20IEEECVF%20Conference%20on%20Computer%20Vision%20and%20Pattern%20Recognition%20Workshops%20%28CVPRW%29.pdf)

## 0. 最终结论

第一阶段只回答一个问题：

> 在已知二维亚纤芯运动、已知理想纤芯几何、非相干强度成像条件下，低速曝光 frame 与理想异步事件联合起来，是否比相同 frame 预算的 frame-only 方法更可靠地恢复高分辨率、无蜂窝的远端物体？

第一阶段不追求完整硬件仿真，而追求一个最小、物理正确、可诊断的闭环：

```text
无蜂窝真值 O
    → 已知二维连续运动 s(t)
    → 圆形纤芯有限孔径积分
    → 每芯连续强度 c_i(t)
    ├─ 曝光积分 → 低速 core-frame
    └─ log 阈值交叉 → 理想事件流
    → frame + event 联合反演 O
```

最终采用以下原则：

1. 用 `550 µm` 作为有效图像圆直径，`600 µm` 是光纤外径，不混用。
2. 第一轮只模拟中央 `128×128 µm` 局部区域，约 930 根纤芯，不直接上完整 15k cores。
3. 单根纤芯使用直径 `2.9 µm` 的归一化圆盘孔径，而不是因“可微”理由改成 Gaussian。
4. 使用已知的二维四相位连续轨迹；水平单向运动只用于单元测试，不作为最终超分辨实验。
5. APS frame 由连续强度做曝光积分产生；v2e 不负责生成物理 APS frame。
6. 第一轮事件由透明、可验证的理想 log-threshold 模型生成；闭环通过后再用 v2e 做真实性压力测试。
7. 不在固定近端蜂窝的传感器坐标上直接套普通平移 IWE，而使用完整光纤算子对运动的响应。
8. 先固定运动和光纤参数，只优化物体；未知运动、真实 core map、GRIN、继像、DAVIS 像素和噪声都放到下一阶段。
9. “2×”表示相对 4.5 µm 芯间距使用 2.25 µm 重建网格，不自动等于真实光学分辨率提高 2 倍。
10. exact-model 成功只证明代码闭环；第一阶段结束前必须追加小幅模型失配测试。

## 1. 两份方案的比较与取舍

### 1.1 豆包方案中合理、值得保留的部分

#### 1. 分阶段思路正确

先用理想圆芯和规则排布验证原理，再从真实端面图提取 core centers、尺寸、椭圆度和 gain，这个顺序合理。它能避免第一轮同时面对几何误差、照明不均、灰尘、坏芯和传感器噪声。

#### 2. 正确区分了“圆形纤芯”和“六角排布”

单根纤芯近似圆形，与纤芯中心整体呈六角/近六角排列并不冲突。真实边缘亮斑看起来椭圆，可能来自端面倾斜、轴外像差、投影、裁切或聚焦误差，不能据此直接否定中心排布规律。

#### 3. 光路模块化是对的

把系统拆成物体运动、远端成像/GRIN、纤芯采样、近端光斑、继像和传感器，有利于以后逐个标定，也符合 NeuroSR 的可插拔前向算子思想。

#### 4. 使用连续高时间分辨率强度生成事件是对的

事件必须从连续辐照度产生，而不是从最终低帧率 APS 视频“创造”。豆包方案明确提出先生成高帧率强度序列再交给事件模拟器，这一方向正确。

#### 5. 初期不加暗角是合理的

暗角、边缘耦合和 illumination flat-field 主要属于实验对齐参数。第一阶段先设均匀照明，可以减少不可辨识变量。

#### 6. 先做 2×、使用 USAF 和消融对照是合理的

2× 比直接追求 4× 更稳妥；USAF、PSNR、SSIM、蜂窝频谱和 frame-only/event-only/joint 对照都应保留。

### 1.2 豆包方案中不合理或需要修正的部分

| 问题 | 严重性 | 原因 | 最终处理 |
|---|---|---|---|
| 把 600 µm 当有效传像直径 | 高 | 厂商资料中有效 image circle 为 `550±30 µm`，600 µm 是 fiber diameter | 使用 550 µm 有效圆；600 µm 只记作外径 |
| 同时固定 15000 cores、550/600 µm 和 4.5 µm pitch | 中 | 三者受几何面积约束，不能独立精确指定 | 第一阶段固定 pitch 和局部 ROI；完整束时由几何实际生成数量 |
| 认为二值 mask/硬圆孔不可微 | 高 | 固定 mask 乘法、固定圆孔卷积和线性采样对物体 `O` 完全可微；只有对半径/边界参数本身求导才有困难 | forward 使用物理圆盘孔径；不因可微性强行换 Gaussian |
| 用 Gaussian core kernel 等同真实圆芯 | 中 | Gaussian 与圆盘的 MTF 不同，会改变可恢复频率；同一 Gaussian 同时用于 forward/inverse 容易形成 inverse crime | forward 用圆盘；Gaussian 只作失配/消融模型 |
| 把出射 Gaussian 扩散称为“芯间串扰” | 高 | 近端亮斑 PSF/继像模糊与光纤内部 core-to-core 能量耦合是两件事 | 串扰用 core signal 稀疏混合矩阵；近端亮斑单独用 PSF 表示，第一阶段两者都关闭 |
| 经验指定透射率 0.85、GRIN σ=0.8 µm、继像 σ=1.5 µm | 中 | 没有当前硬件标定依据，整体透射率还与 APS gain 存在尺度歧义 | 第一阶段设 identity/unit gain；以后由实测 PSF/flat-field 替换 |
| 推荐水平单向运动作为完整第一阶段 | 高 | 事件主要约束沿运动方向的梯度，二维图像会出现方向性欠约束；单向扫描也不能充分覆盖六角晶格相位 | 水平运动仅做符号测试；正式闭环使用二维四相位轨迹 |
| 总时长只取 25 ms、仅一个 frame period | 中 | 对运动覆盖、低频锚和事件统计都过于苛刻，不利于第一次排错 | 第一阶段使用 100 ms、两个曝光 frame，再逐步减少 frame 数 |
| 将 40 fps 等同于 25 ms exposure | 高 | 25 ms 是 frame period；实际曝光可短于 frame period | frame rate 与 exposure 分开保存和建模 |
| 认为 v2e 可生成同步物理 APS frame | 高 | v2e 的核心是从输入帧生成 DVS events；`dvs_exposure` 是事件预览视频的累积方式，不是 APS 曝光模型 | APS 由共享连续强度单独曝光积分；v2e 仅生成/校验事件 |
| 将 v2e 的事件预览帧当 APS frame | 高 | event accumulation frame 与光强 APS frame 物理含义完全不同 | 两者在文件名、数据结构和损失中严格分开 |
| 写默认阈值约 0.15 | 中 | 当前本地源码默认 ON/OFF threshold 都是 0.2 | baseline 用 0.2，另扫 0.10–0.25 |
| 1000 fps/1 ms 足以匹配 300 Hz photoreceptor | 中 | 300 Hz 一阶低通时间常数约 0.53 ms，1 ms 步长偏粗；多事件时还会产生时间堆叠误差 | 理想模型用 0.1 ms 并插值阈值交点；v2e realism 另做步长收敛测试 |
| 认为固定蜂窝不干扰普通事件运动估计 | 高 | 传感器上的 core spots 固定，变化的是每芯强度；普通 IWE 假设事件位置随图像特征运动，周期蜂窝还可能造成混叠 | 使用 fibre-aware 运动算子；第一阶段先给真值运动 |
| 直接使用传感器面 `-∇log fΘ(O)·v` | 高 | 固定纤芯采样通常不与平移交换，传感器图像不是简单的整体平移 | 使用 `d/dt log HΘ(O,s(t))` 或等价的 core-domain 累积事件约束 |
| 说 LED 红光使边缘衰减更平缓 | 中 | 光谱宽度与角度分布是不同属性；边缘耦合取决于照明/GRIN/NA/几何，不能仅由“红光 LED”推出 | 第一阶段关闭；实验阶段实测 flat-field |
| 说暗角对事件“几乎无影响” | 中 | 理想纯乘性、静态、无噪声时 log 导数可抵消；但低照度会改变带宽、噪声和有效阈值，运动照明也会触发事件 | 只在理想模型中忽略，真实性阶段显式测试 |
| 把径向平均功率谱称为 MTF | 中 | 功率谱相似性不是系统 MTF | MTF 用 slanted-edge/正弦靶；USAF 用 contrast transfer；功率谱仅作补充 |
| “2×像素”直接等价“2×分辨率” | 高 | 输出网格加密不保证跨越芯孔径和光学 MTF 限制 | 报告可分辨线宽/空间频率，不只报告输出尺寸 |
| 镜像 padding | 低至中 | 镜像边界会引入不存在的结构和事件 | 使用带 margin 的大图，最后只评价 valid crop |

### 1.3 我上一版方案中需要修正的部分

上一版在物理方向上比豆包方案更谨慎，但也不够适合作为“马上实施的第一阶段”：

1. **范围过大**：同时讨论完整 15k fibre、DAVIS346、v2e、未知运动、噪声、真实标定和模型失配，像总路线图，不像第一阶段任务书。
2. **没有做唯一技术选择**：既讲 core-domain，又讲 sensor-domain，虽提出先做 core-domain，但没有把它明确规定为第一阶段唯一权威观测域。
3. **fibre-aware IWE 写得过于确定**：它是根据论文原则提出的适配方法，不是论文已经验证的标准模块；具体的 event-to-core 权重和统计模型仍需实验验证。
4. **第一阶段轨迹仍偏复杂**：Lissajous 适合后续稳健性测试，但第一次排错更适合已知、确定性的四相位闭合轨迹。
5. **参数扫描太宽**：大量噪声、串扰、PSF 和 DAVIS 参数会稀释第一阶段的核心结论。
6. **过早讨论 4×**：当前最重要的是验证 2× 是否有独立信息增益；4× 只应在 2× 稳定后讨论。
7. **直接事件梯度与 IWE 的关系不够落地**：需要明确第一阶段实际计算哪一个 loss，而不是只写通用目标。
8. **对 exact-model 闭环的局限强调了，但没有把 mismatch 设为第一阶段退出门槛**。

因此，本文件将第一阶段收敛为：**局部 core-domain、圆盘孔径、已知二维轨迹、两帧低频锚、理想事件、逐事件 threshold-timestamp 保真、只优化 O。**

## 2. 第一阶段的物理假设和边界

### 2.1 保留的物理效应

- 非相干、线性、非负强度物体；
- 物体像相对远端 core lattice 的二维平移；
- 每根芯的有限圆形孔径积分；
- frame 的真实曝光积分；
- event 的 log-intensity threshold crossing；
- frame 和 event 共用同一个连续 core intensity；
- hex lattice 的亚纤芯相位覆盖；
- 低频 frame 与差分 event 的联合反演。

### 2.2 第一阶段明确不加入的效应

- GRIN 放大率、畸变、像差和暗角；
- 完整 550 µm 有效圆和全部 15k cores；
- 近端 core spot 渲染、继像物镜和 sensor pixel integration；
- DAVIS346 的 346×260 网格和 18.5 µm pixel pitch；
- core gain mismatch、坏芯、串扰、弯曲和 speckle；
- APS photon/read noise、量化、饱和和 auto exposure；
- v2e 的 finite bandwidth、threshold mismatch、leak、shot noise 和 refractory；
- 未知 motion 和 contrast-maximization；
- 联合优化 core positions、PSF、threshold 等系统参数。

这些不是不重要，而是不能在第一次闭环里同时加入。第一阶段通过后，它们才按一项一项的消融顺序进入。

### 2.3 为什么第一阶段采用 core-domain

真实近端一个 core spot 常覆盖多个 event sensor pixels，这些 pixels 对同一根纤芯的强度变化高度相关，并不是独立的物体采样。如果第一步就加入近端 spot 和 DAVIS pixels，会混入以下问题：

- 一个 core 的多像素事件重复计权；
- spot PSF 与真正芯间串扰混淆；
- sensor sampling 与 fibre sampling 的分辨率混淆；
- 普通 IWE 和固定蜂窝坐标不匹配；
- 重建失败时难以判断是信息不足还是传感器渲染错误。

因此第一阶段把每根 core 视为一个理想的独立强度通道。输出仍可渲染成蜂窝图用于查看，但重建损失使用每芯强度和每芯事件，渲染图不是额外测量。

## 3. 硬件参数与几何一致性

Fujikura 当前数据表给出：

| 参数 | 采用值 | 说明 |
|---|---:|---|
| 有效 image circle | 550 µm | 容差 ±30 µm |
| fiber diameter | 600 µm | 容差 ±30 µm，不是有效图像圆 |
| 标称 picture elements | 15000 | 容差 ±1500 |
| core pitch | 4.5 µm | 用户给定工作值 |
| core diameter | 2.9 µm | 用户给定工作值 |

[Fujikura 官方 FIGH N 型数据表](https://www.optic-product.fujikura.com/wp-content/uploads/2024/07/FPE29A.pdf)

理想六角晶格单元面积为

$$
A_{cell}=\frac{\sqrt3}{2}p^2.
$$

若有效圆直径为 550 µm、pitch 为 4.5 µm，则理想纤芯数近似为

$$
N\approx\frac{\pi(550/2)^2}{(\sqrt3/2)(4.5)^2}\approx13548.
$$

这接近厂商 13500–16500 的容差下限。若严格要求圆内约 15000 根，理想 pitch 应约为 4.28 µm。第一阶段使用局部无限 hex lattice，不需要人为强行同时满足三个数值；完整 bundle 仿真时再处理圆边界和实际总数。

## 4. 第一阶段统一前向模型

### 4.1 坐标和未知量

| 符号 | 含义 |
|---|---|
| `O(u)` | 待恢复的高分辨率无蜂窝物体强度，`O≥0` |
| `u=(u_x,u_y)` | 远端光纤面物理坐标，单位 µm |
| `r_i` | 第 `i` 根 core 的远端中心 |
| `s(t)` | 物体像相对固定 core lattice 的已知二维位移 |
| `A_i(u-r_i)` | 第 `i` 根 core 的归一化圆盘孔径 |
| `c_i(t)` | 第 `i` 根 core 的连续输出强度 |
| `L_i(t)` | 第 `i` 根 core 的 log intensity |

第一阶段唯一优化变量为 `O`。`r_i`、core diameter、motion、event threshold 和 exposure 全部固定为仿真真值。

### 4.2 物体运动

约定正位移 `s(t)` 时，core `i` 实际采样到的物体坐标为 `r_i-s(t)`：

$$
J(\mathbf u,t)=O(\mathbf u-\mathbf s(t)).
$$

不使用镜像 padding。输入物体比评价 ROI 大，运动和 core aperture 始终落在有效数据内；最后只评价中央 valid crop。

### 4.3 圆形 core aperture

第 `i` 根 core 的归一化孔径为

$$
A_i(\mathbf u)=
\frac{1}{\pi(d/2)^2}
\mathbf 1_{\|\mathbf u-\mathbf r_i\|\le d/2},
\qquad d=2.9\ \mu\mathrm m.
$$

core 输出强度为

$$
c_i(t)=
\int A_i(\mathbf u)
O(\mathbf u-\mathbf s(t))\,d\mathbf u.
$$

实现建议：

1. 在 0.5 µm/px forward grid 上预计算归一化 subpixel disk kernels；
2. 先对 `O_t` 做 disk convolution，再在 core centers 处用双线性采样；
3. 或预计算稀疏采样矩阵 `S(s_t)`，使 `c(t)=S(s_t)O`；
4. 固定圆盘卷积和双线性采样对 `O` 可微，不需要改成 Gaussian。

### 4.4 core-frame 曝光模型

第 `m` 个 frame 的曝光区间是 `[t_m^0,t_m^1]`，不是单个时间戳：

$$
y_{m,i}=\frac{1}{T_{exp}}
\int_{t_m^0}^{t_m^1}c_i(t)\,dt,
\qquad T_{exp}=t_m^1-t_m^0.
$$

第一阶段使用无噪声 float32 线性强度。离散实现用 dense time samples 做 trapezoidal integration，并保存曝光起止时间。

权威 frame 数据为 `[M,N_core]` 的 `core_values`。为便于肉眼观察，可额外将这些值 splat 到二维 hex lattice 形成蜂窝图，但该图只用于可视化。

### 4.5 理想事件生成

先定义

$$
L_i(t)=\log\big(c_i(t)+I_0\big),
$$

其中输入强度归一化到 `[0.05,1]`，baseline 使用 `I_0=10^{-3}`。

每根 core 维护自己的参考 log intensity `L_i^{ref}`。当

$$
L_i(t)-L_i^{ref}\ge c_+
$$

时产生 ON event；当

$$
L_i(t)-L_i^{ref}\le-c_-
$$

时产生 OFF event。每产生一个事件后，参考值增加或减少相应阈值。若一个时间步跨越多个阈值，必须生成多个事件；事件时刻在两个 dense samples 之间做线性插值。

第一阶段 baseline：

$$
c_+=c_-=0.2.
$$

事件保存为

```text
(timestamp_us, core_id, x_core_um, y_core_um, polarity)
```

其中 polarity 推荐内部使用 `-1/+1`，导出兼容文件时再映射为 `0/1`。

### 4.6 frame 和 event 的共享关系

必须先计算唯一的连续 `c_i(t)`，再从它分叉：

```text
c_i(t)
  ├─ exposure integration → y_frame
  └─ log + threshold crossing → events
```

禁止分别生成两套运动序列，禁止从最终 frame 再反推 events，禁止把 event accumulation image 当作 APS frame。

## 5. 第一阶段的事件保真项

### 5.1 不直接使用普通传感器平移 IWE

论文的简单形式

$$
\partial_tL\approx-\nabla L\cdot\mathbf v
$$

要求传感器图像近似整体平移。多芯光纤远端运动时，近端 core locations 固定，变化的是 `c_i(t)`，因此在近端图像上直接做 `x'_k=x_k-v(t_k-t_0)` 不正确。

完整的 fibre-aware 一阶关系是

$$
\frac{d}{dt}\log\mathcal H_{fiber}
\big(O,\mathbf s(t)\big)
=
\frac{\partial\log\mathcal H_{fiber}}{\partial\mathbf s}
\dot{\mathbf s}(t).
$$

第一阶段 motion 已知，所以不需要先解决 fibre-aware contrast maximization。

### 5.2 主损失：逐事件 threshold-timestamp fidelity

对第 `i` 根 core 的第 `k` 个事件，令 `t^-_{i,k}` 表示该 core 的前一个事件时刻；对第一个事件，使用序列初始时刻 `t_0`。理想、连续、无噪声事件模型在触发时满足

$$
L_i(t_{i,k};O)-L_i(t^-_{i,k};O)=p_{i,k}c.
$$

因此第一阶段主事件损失定义为

$$
D_E^{time}(O)=
\operatorname{mean}_{i,k}
\rho_{Huber}
\left(
\frac{
L_i(t_{i,k};O)-L_i(t^-_{i,k};O)-p_{i,k}c
}{c}
\right).
$$

`L_i(t;O)` 在观测 event timestamp 处由可微的连续运动/时间插值得到。这个形式有四个优点：

1. 直接使用论文和事件电路的 `ΔL=pc` 触发条件；
2. 不会把“发生了亚阈值变化但没有事件”的小时间窗错误约束为零变化；
3. 不错误假设近端蜂窝整体平移；
4. 在 ideal forward 下可以逐事件精确验证 polarity、threshold 和 timestamp。

注意：必须明确模拟器的初始化方式。本文规定所有 core 在 `t_0` 将 reference 初始化为 `L_i(t_0)`，所以第一个事件也可写入上述等式。

### 5.3 次要诊断：时间窗累计事件

为了和论文的 temporally accumulated event statistic 对照，仍保存时间窗 `w=[t_a,t_b]` 内的

$$
b_{E,i,w}=c\sum_{k\in(i,w)}p_k.
$$

它近似 `L_i(t_b)-L_i(t_a)`，但两端都存在未触发的 residual contrast，因此不能在短窗口中把二者当成严格相等；无事件窗口尤其可能包含小于 threshold 的真实变化。时间窗累计只用于：

- event count、ON/OFF balance 和 event coverage 诊断；
- 论文式 Gaussian aggregate 的次要消融；
- v2e noisy events 下比逐事件等式更稳健的后续近似。

baseline diagnostic bin 取 `2 ms`，另保存 `1、5 ms`。若用于 loss，应显式建模 residual/dead-zone 或基于统计方差加权，不能使用无容差的普通 L2。

### 5.4 联合重建目标

frame loss 使用归一化 L2：

$$
D_F(O)=
\operatorname{mean}_{m,i}
\left(
\frac{\widehat y_{m,i}(O)-y_{m,i}}
{\operatorname{mean}(y)+\epsilon}
\right)^2.
$$

第一阶段目标为

$$
O^*=\arg\min_{O\ge0}
D_F(O)+\alpha D_E^{time}(O)
+\beta_{TV}\operatorname{TV}(O).
$$

实现建议：

- 用 `O=softplus(Z)` 保证非负；
- 因两个 fidelity 已归一化，`α=1` 作为起点，再扫 `0.25、0.5、1、2`；
- `β_TV` 从使初始 TV gradient norm 约为 data gradient norm 的 1% 开始，而不是硬编码一个跨数据集通用值；
- Adam 只用于快速初值，最终可用 L-BFGS/Adam 精细收敛并检查 data residual；
- 第一阶段不优化 motion、threshold、core size 和 gain。

## 6. 确定性的二维运动设计

### 6.1 hex lattice 基向量

$$
\mathbf a_1=(p,0),
\qquad
\mathbf a_2=\left(\frac p2,\frac{\sqrt3p}{2}\right),
\qquad p=4.5\ \mu\mathrm m.
$$

### 6.2 2× 四相位闭合轨迹

在 100 ms 内依次经过：

| 时间 | 位移节点 |
|---:|---|
| 0 ms | `s0=(0,0)` |
| 25 ms | `s1=a1/2=(2.25,0) µm` |
| 50 ms | `s2=(a1+a2)/2=(3.375,1.949) µm` |
| 75 ms | `s3=a2/2=(1.125,1.949) µm` |
| 100 ms | `s4=(0,0)` |

相邻节点间使用 minimum-jerk 曲线，而不是瞬时跳变：

$$
q(\tau)=10\tau^3-15\tau^4+6\tau^5,
\qquad \tau\in[0,1].
$$

$$
\mathbf s(t)=\mathbf s_j+
q(\tau)(\mathbf s_{j+1}-\mathbf s_j).
$$

该轨迹覆盖 hex unit cell 的四个 2× 相位，同时包含两个方向。总路径约 9 µm，100 ms 内平均路径速度约 90 µm/s，峰值速度由 minimum-jerk 曲线决定。

### 6.3 单方向只做单元测试

另设一个 `25 ms、vx=180 µm/s、vy=0` 的水平匀速样例，只用于验证：

- 平移正负号；
- 梯度方向和 ON/OFF polarity；
- 一帧周期移动一个 pitch 的量级；
- event timestamp interpolation。

它不作为 2D 重建主实验。

## 7. 第一阶段输入数据与参数

### 7.1 必需输入

#### A. 真值物体

至少准备五类：

1. 单点、水平/垂直/斜线，用于单元测试；
2. 不同方向正弦条纹，用于方向可辨识性；
3. 本地 `分辨率板.png`，重新赋予明确物理尺度；
4. 细纤维、交叉纤维、弯曲纤维合成图；
5. 至少 5 张无蜂窝真实显微图作为域内测试。

要求：

- 线性 float32，不使用 sRGB gamma；
- 归一化后强度建议 `[0.05,1]`；
- 每张图保存 `um_per_pixel`；
- 训练/调参图与最终报告图分开。

#### B. fibre config

```yaml
active_diameter_um: 550.0
outer_diameter_um: 600.0
core_pitch_um: 4.5
core_diameter_um: 2.9
lattice: hexagonal
local_roi_um: [128.0, 128.0]
core_gain: 1.0
crosstalk: 0.0
```

#### C. motion config

保存所有 trajectory nodes、插值方式、总时长和时间零点，不只保存速度。

#### D. sensor config

第一阶段只包含 frame exposure 和 ideal event threshold，不包含 DAVIS pixel geometry。

### 7.2 baseline 数值

| 参数 | baseline | 扫描值/备注 |
|---|---:|---|
| 输入物体物理范围 | 160×160 µm | 为 128×128 µm valid ROI 留 margin |
| forward grid | 0.5 µm/px | 与 0.25、1.0 µm/px 做收敛检查 |
| valid ROI | 128×128 µm | 中央区域，约 930 个 hex cores |
| core pitch | 4.5 µm | 固定 |
| core diameter | 2.9 µm | 固定圆盘 |
| reconstruction grid | 2.25 µm/px | 2× sampling grid |
| sequence duration | 100 ms | 固定四相位闭合轨迹 |
| dense time step | 0.1 ms | 1001 个时刻；另测 0.05、0.2 ms |
| frame rate | 20 fps | 在 100 ms 内得到 2 个曝光 frame |
| exposure | 20 ms | 与 50 ms frame period 分开 |
| frame exposure windows | `[0,20]`、`[50,70]` ms | 明确保存起止时间 |
| event threshold | ON=OFF=0.2 | 扫 0.10、0.15、0.20、0.25 |
| diagnostic event bin | 2 ms | 保存 1、2、5 ms；不作为 baseline 主损失 |
| event refractory | 0 | 第一阶段关闭 |
| threshold mismatch | 0 | exact-model 闭环关闭 |
| frame/event noise | 0 | exact-model 闭环关闭 |
| random seeds | 分模块固定 | object、geometry、noise 分开保存 |

### 7.3 为什么 forward grid 取 0.5 µm/px

2.9 µm core diameter 在 0.5 µm 网格上约跨 5.8 pixels，比 1 µm 网格的 2.9 pixels 更适合圆盘积分和亚像素运动。必须通过 0.25/0.5/1.0 µm 的结果收敛性确认，而不能只凭“每芯至少 3 pixels”判断足够。

## 8. 输出数据格式

建议每个样本保存一个 HDF5：

```text
/truth/object_forward_grid        [Hf,Wf]
/truth/object_eval_grid           [Hr,Wr]
/truth/motion_dense               [T,3] = t,dx,dy
/truth/core_xy_um                 [N,2]
/truth/core_signal                [T,N]

/frames/core_values               [M,N]
/frames/t_start_s                 [M]
/frames/t_end_s                   [M]
/frames/rendered_honeycomb        [M,Hv,Wv]  # 仅可视化

/events/data                      [Ne,5]
                                  # t_us,core_id,x_um,y_um,polarity
/events/bin_edges_s               [B+1]
/events/signed_counts             [B,N]
/events/on_counts                 [B,N]
/events/off_counts                [B,N]

/meta/config_yaml
/meta/seeds
/meta/code_version
```

必须保存 `core_signal`，因为它是定位 forward、frame integration 和 event trigger 错误的中间真值。

## 9. 实施顺序与退出门槛

### P1.0：算子单元测试

必须通过：

1. hex pitch、row spacing 和 core 数量估计正确；
2. 均匀物体产生一致 core signals；
3. 静止物体产生零个理想 events；
4. 水平线性 ramp 的事件 polarity 和解析结果一致；
5. 一个 dense step 跨多个 thresholds 时事件数量正确；
6. frame 数值积分与高精度参考一致；
7. PyTorch autograd 与 finite difference 的相对误差建议 `<10^-4`；
8. forward 的位移正负号与重建使用的采样坐标一致。

### P1.1：已知运动、无噪声 exact-model 闭环

对每个对象分别运行：

1. 单 frame + core interpolation；
2. 两 frame 的 frame-only reconstruction；
3. event-only reconstruction；
4. 两 frame + events joint reconstruction；
5. ground-truth object。

此阶段只优化 `O`，所有物理参数使用真值。

### P1.2：信息增益消融

至少完成：

- 水平单向轨迹 vs 二维四相位轨迹；
- 1 个 frame vs 2 个 frames；
- threshold `0.10/0.15/0.20/0.25`；
- threshold-timestamp loss vs binned aggregate loss；
- diagnostic event bin `1/2/5 ms`；
- disk aperture vs point-sampling 错误模型；
- `α=0`、event-only、joint；
- 同样 frame 数量、同样曝光时间和同样运动窗口下比较。

### P1.3：小幅模型失配

exact-model 通过后，至少加入三项隐藏 mismatch：

1. forward core diameter 2.9 µm，inverse 使用 2.8 或 3.0 µm；
2. forward core centers 加 `σ=0.05–0.10 µm` 小扰动，inverse 仍用理想 lattice；
3. forward event thresholds 加 `σ=0.01`，inverse 使用全局 0.2；
4. 可选：inverse motion nodes 加 0.05 µm 初始误差，但第一阶段仍不联合优化 motion。

这一步通过后，才能说方案不仅是 exact-model 的数值自证。

### P1.4：工程 go/no-go 标准

以下是工程决策线，不是理论定理：

1. 在至少 5 类对象、多个固定 seeds 上，joint 相对最佳同预算 frame-only 的 PSNR/SSIM 和高频指标呈稳定改善，而不是只在一张 USAF 图上有效；
2. 建议目标：平均 PSNR 至少提高约 1 dB，且没有明显结构幻觉；
3. USAF/正弦靶至少多恢复一个稳定可辨的更细频率级别；
4. hex lattice 频率附近的伪影能量相对 raw fibre frame 至少下降 6 dB；
5. joint 重新投影得到的 frames/events 与观测一致；
6. 小幅模型失配后仍优于最佳 frame-only；
7. 若 joint 只比单 frame 好、但不如两 frame frame-only，则不能宣称事件提供了独立增益。

若 P1.1 在 oracle motion、无噪声下失败，应停止添加 v2e、DAVIS 和真实 mask，优先检查可辨识性、event loss、轨迹覆盖、core aperture 和坐标符号。

## 10. 正确的评价指标

### 10.1 图像指标

- PSNR、SSIM：在相同 valid crop、相同强度归一化规则下计算；
- 细纤维宽度、连通性、交叉点保持；
- 重建光度均值和对比度，防止 event-only 的积分常数漂移。

### 10.2 分辨率指标

- slanted-edge ESF/LSF/MTF50/MTF10；
- 正弦条纹的 contrast transfer function；
- USAF 可辨 group/element 和线对对比度；
- 2D power spectrum 只作为频谱结构辅助，不称为 MTF。

### 10.3 蜂窝指标

在 hex reciprocal lattice 的主峰邻域计算

$$
R_{hex}=10\log_{10}
\frac{E_{hex\ peaks}}{E_{local\ background}}.
$$

报告 raw fibre render、frame-only、event-only 和 joint 的 `R_hex`，同时检查是否通过过强低通“消除蜂窝但也消除真实细节”。

### 10.4 event consistency

- 总 event count、ON/OFF ratio、活跃 core 比例；
- 每芯 event count 分布；
- 从事件累计得到的 log change 与真值 `L_i(t_b)-L_i(t_a)`；
- predicted events 与 observed events 的 count/polarity residual；
- dt 和 event-bin 收敛性。

## 11. v2e 在第一阶段中的正确位置

v2e 不是第一阶段的 APS 模拟器。当前工程中：

- `--dvs_exposure` 控制 event accumulation 预览帧，不是 APS exposure；
- `--ddd_output` 可以保存输入/源 frame 及事件索引，但这些仍不是带独立 APS 曝光/噪声模型的仿真；
- 当前本地默认 `pos_thres=neg_thres=0.2`、`sigma_thres=0.03`、`cutoff_hz=300`、`leak_rate_hz=0.01`、`shot_noise_rate_hz=0.001`；
- 低时间采样率配 300 Hz photoreceptor 会欠采样。

第一阶段正确用法：

1. 先用本文件的理想 threshold-crossing generator 完成 P1.0–P1.2；
2. 再把相同 `core_signal` 渲染成固定 core spots 的高时间分辨率强度图；
3. 将该图序列交给 `EventEmulator`/v2e，关闭 SuperSloMo；
4. APS frames 仍由共享连续强度独立曝光积分；
5. 比较 ideal events 与 v2e events 下重建退化量；
6. 这只是 realism cross-check，不替代 core-domain 主结论。

由于近端一个 core spot 会覆盖多个 sensor pixels，v2e cross-check 必须按 core mask 汇总或归一化事件，不能把同一 core 的多个 pixels 当作独立空间样本。

## 12. 第一阶段之后才做什么

只有 P1.4 通过后，第二阶段才按以下顺序展开：

1. 完整 550 µm bundle 与实际 core 数；
2. 真实近端 core center map、gain 和 elliptical spot PSF；
3. 近端 relay 与 DAVIS sensor pixels；
4. v2e finite bandwidth、threshold mismatch、leak、shot noise 和 refractory；
5. APS photon/read noise 与 saturation；
6. fibre-aware motion initialization；
7. 小范围 joint motion refinement；
8. GRIN/illumination flat-field/畸变；
9. 真实红光 LED 和真实采集标定。

真实 mask 需要拆成两类参数：

- **几何参数**：core centers、有效半径、椭圆轴、distal-to-proximal mapping，通常比照明稳定；
- **光度参数**：gain、flat-field、spot PSF、背景和暗角，依赖波长、焦距、对焦、照明角度和传感器设置。

固定二值 mask 并非“不可微”，但直接把某一次特定照明下的灰度端面图当作永久 mask 会把几何和光度混在一起。实验阶段应从标定图提取参数化算子，而不是简单复制灰度图。

## 13. 第一阶段推荐目录结构

```text
fibre_phase1/
  configs/
    fibre_local_ideal.yaml
    motion_hex_2x.yaml
    sensor_core_ideal.yaml
  data/
    objects/
    simulated/
  forward/
    hex_lattice.py
    disk_aperture.py
    motion.py
    core_signal.py
    frame_integrator.py
    ideal_event.py
  reconstruction/
    event_bins.py
    losses.py
    optimize_object.py
  evaluation/
    image_metrics.py
    resolution_metrics.py
    honeycomb_metric.py
    event_consistency.py
  scripts/
    simulate_phase1.py
    reconstruct_phase1.py
    run_ablation_phase1.py
  tests/
```

不要直接修改 `v2e/` 的核心文件来实现光纤 forward。新包调用 v2e 的事件模拟能力即可，便于区分上游代码、本地已有修改和新研究代码。

## 14. 最小可执行任务清单

按以下顺序编码，任何一步失败都先修复再继续：

1. 生成 128×128 µm local hex core centers；
2. 实现归一化 2.9 µm disk aperture；
3. 实现带 valid margin 的连续二维运动采样；
4. 输出并检查 `core_signal[T,N]`；
5. 实现两个 20 ms core-frame 曝光积分；
6. 实现多阈值、亚时间步插值的 ideal event generator；
7. 保存统一 HDF5 和全部 config/seeds；
8. 完成 P1.0 全部单元测试；
9. 实现 frame-only 重建；
10. 实现逐事件 threshold-timestamp loss；
11. 实现 binned event 统计诊断；
12. 实现 joint reconstruction；
13. 跑 5 类对象和所有消融；
14. 加入三项小幅模型失配；
15. 根据 P1.4 决定进入 sensor/v2e 阶段，或返回修改可辨识性设计。

## 15. 一句话方案

> 第一阶段先把 FIGH-15-600N 看成已知的六角排列圆孔采样阵列，在中央局部区域用已知二维四相位连续运动产生每芯连续强度，由同一强度分别积分出两个低速 frame、阈值触发出理想 events，再用物理圆孔 forward、frame fidelity 和 fibre-aware threshold-timestamp fidelity 只优化无蜂窝物体；只有它在同预算 frame-only、消融和小幅模型失配下都稳定占优，才值得加入 v2e、DAVIS、真实 mask 和未知运动。
